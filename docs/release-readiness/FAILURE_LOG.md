# Failure Log

Date: 2026-06-18

## 2026-06-21 Post-Merge Deploy And Smoke

| Failure | Reproduction | Root Cause | Resolution | Current Status |
|---|---|---|---|---|
| Render backend deploy failed on merge commit `4bda5691...` | Render deploys `dep-d8rm7g28qa3s73a4mplg` and `dep-d8rm8egjs32c73c761ig` reached `update_failed` | Production startup now requires an explicit `BRASSTUNE_AUTH_MODE` in `APP_ENV=production`; the live Render service was missing that env var even though `render.yaml` declares `BRASSTUNE_AUTH_MODE=disabled`. | Set service env `BRASSTUNE_AUTH_MODE=disabled` through the Render API without printing secret values, then triggered deploy hook. | Fixed: Render deploy `dep-d8rmafreo5us73di4as0` is live for `4bda5691...`. |
| Hosted smoke rejected normal WebSocket app check after backend deploy | `BRASSTUNE_WEB_BASE_URL=https://brass-tune.vercel.app ... npm run smoke:hosted` failed `WebSocket /ws/pitch app-level response` with `WebSocket origin is not allowed.` | The smoke script used Node's built-in `WebSocket`, which did not send the production web `Origin`; real browsers do send this header and the backend correctly requires it in production. | Updated `scripts/hosted-smoke.mjs` to use the raw WebSocket probe for the normal app-level check as well, sending `Origin: https://brass-tune.vercel.app` and a masked client `ping` frame. | Fixed: hosted production smoke passed all 7 checks. |

## 2026-06-21 Follow-Up Patch

| Failure | Reproduction | Root Cause | Resolution | Current Status |
|---|---|---|---|---|
| Direct requirements-file `pip-audit` crashed while creating its temporary resolver venv | `cd backend && .venv/bin/python -m pip_audit -r requirements.txt -r requirements-dev.txt` | The local bundled Python 3.12 resolver path hit an `ensurepip` SIGABRT inside `pip-audit`'s temporary venv creation. The failure happened before vulnerability analysis. | Resolved requirements with `uv pip compile --python /Users/aryasalem/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 requirements-dev.txt -o /tmp/brasstune-pip-audit-requirements.txt` and audited the pinned output with `.venv/bin/python -m pip_audit --no-deps --disable-pip -r /tmp/brasstune-pip-audit-requirements.txt`. | Workaround passed; no known vulnerabilities found. |
| Device simulation hung and partially rewrote tracked screenshots | `cd frontend && npm run simulate:devices` | Chromium stayed active silently for about six minutes during the screenshot traversal and left partial screenshot deletions/modifications. | Stopped the process and restored `docs/assets/device-simulation/` to the previous tracked state. | Device simulation skipped for this patch; no screenshot changes kept. |
| Production smoke still fails WebSocket hardening before merge/deploy | `BRASSTUNE_WEB_BASE_URL=https://brass-tune.vercel.app BRASSTUNE_WEB_ACCESS_URL=https://brass-tune.vercel.app BRASSTUNE_API_BASE_URL=https://brasstune.onrender.com BRASSTUNE_WS_BASE_URL=wss://brasstune.onrender.com npm run smoke:hosted` | Production Render has not yet been redeployed with the branch WebSocket hardening. | Local backend tests pass the hardened behavior; merge/deploy must update Render before production smoke can pass. | Red production gate: 5 passed, 2 failed. |
| XcodeBuildMCP defaults were unavailable in this thread | `session_show_defaults` | The exposed XcodeBuildMCP session had no project, scheme, or simulator defaults and no setter tool was exposed in this thread. | Used shell `xcodebuild` with a dynamically discovered simulator instead. | Native app unit, UI smoke, and Release simulator build passed. |

## 2026-06-21 Final Stabilization Pass

| Failure | Reproduction | Root Cause | Resolution | Current Status |
|---|---|---|---|---|
| Production hosted smoke failed after local stabilization patch | `cd frontend && BRASSTUNE_WEB_BASE_URL=https://brass-tune.vercel.app BRASSTUNE_WEB_ACCESS_URL=https://brass-tune.vercel.app BRASSTUNE_API_BASE_URL=https://brasstune.onrender.com BRASSTUNE_WS_BASE_URL=wss://brasstune.onrender.com npm run smoke:hosted` | Production Vercel is still serving an older app that lacks the current Audio Lab `Cloud sync stream` content, and Firefox/WebKit/mobile-WebKit still logged hosted fetch failures. The local patch is not deployed yet. | No merge or production deploy performed while gates are red. Local repository fixes passed backend/frontend/Swift checks and must be committed, pushed, deployed, and re-smoked before merge/release. | Red merge/deploy gate; 22 passed, 8 failed, 5 skipped in hosted smoke |

## 2026-06-20 Current Run

| Failure | Reproduction | Root Cause | Resolution | Current Status |
|---|---|---|---|---|
| Production startup seeded demo users/sessions by default | Backend audit temp-copy probe with `APP_ENV=production` found demo users/sessions after startup | `app.main.lifespan` always called `seed_demo_data(db)` regardless of deployed environment | Added `BRASSTUNE_SEED_DEMO_DATA` gate: local/test/dev seed by default, deployed envs require explicit opt-in; added startup regression tests | Fixed locally; backend full suite passed `64 passed` |
| Student ensemble roster exposed other students' identities | `GET /api/ensemble/groups/1` as `dev-user-1` returned all members with `user_id`, username, and display name | Ensemble group/member routes serialized the full roster for any active member | Added server-side roster scoping: directors/admins and assistant/director group roles see full roster, ordinary students get only their own redacted membership; added regression tests | Fixed locally; backend targeted and full suites passed |
| Guest audio could be marked saved before local session persistence succeeded | Web/audio audit traced `useAudioRecorder.stopLocal` setting `status='saved'` before `saveGuestSessionFromFrames` writes browser storage | Audio capture and guest session persistence were split across hooks, and the audio hook reported success after data URL creation rather than durable session write | `stopLocal` now leaves audio pending until the session save succeeds; Practice marks local audio saved only after guest persistence returns; quota-failure regression added | Fixed locally; frontend guest storage test and production build passed |
| Mission-style preview smoke did not pass protected preview access through to Playwright | `BRASSTUNE_WEB_ACCESS_URL=<share url> npm run smoke:hosted` did not populate `E2E_VERCEL_SHARE_URL` | Hosted-smoke runner mapped base/API/WS aliases but not the access URL alias used by the release mission | Added `BRASSTUNE_WEB_ACCESS_URL` -> `E2E_VERCEL_SHARE_URL` alias | Fixed locally, but preview browser smoke still depends on working Vercel share-cookie behavior |
| Exact preview browser smoke still hit Vercel Authentication with regenerated share URL | `BRASSTUNE_WEB_BASE_URL=https://brass-tune-icnmvlpd4-aryaswebsites.vercel.app BRASSTUNE_WEB_ACCESS_URL=<share url> ... npm run smoke:hosted` | The protected-preview share response returned HTTP `401` while setting an auth cookie; local Playwright did not convert that first response into a usable authenticated route visit | No code workaround claimed as verified; Vercel connector confirms exact deployment is READY, but browser smoke remains blocked on protected-preview access behavior | Blocked by Vercel preview auth automation |
| Production hosted smoke failed on current production web/backend | `BRASSTUNE_WEB_BASE_URL=https://brass-tune.vercel.app BRASSTUNE_API_BASE_URL=https://brasstune.onrender.com BRASSTUNE_WS_BASE_URL=wss://brasstune.onrender.com npm run smoke:hosted` | Vercel production is still on `0e5eea2`; Render production is healthy but stale for query-token and bad-Origin WebSocket hardening | No production merge/deploy performed; keep PR unmerged until owner-approved Render deploy and production smoke pass | Deployment gate remains open |
| Invalid targeted Vitest command failed | `cd frontend && npm test -- --runInBand frontend/scripts/run-hosted-smoke.mjs` | `--runInBand` is a Jest flag, not accepted by Vitest | Discarded the invalid command and used actual hosted smoke plus normal Vitest commands for validation | Operator error; no product impact |
| Metronome accessibility smoke failed with `aria-prohibited-attr` | `cd frontend && npm run e2e:local`, axe serious smoke on `/metronome` | `aria-label` was applied to a generic `div` without a valid ARIA role on the subdivision visual group | Added `role="group"` to beat/subdivision visual groups | Fixed; full E2E passed `75 passed` |
| Settings release journey could not find `Export account data` | `cd frontend && npm run e2e:local`, settings legal/export/delete journey | Guest copy changed the existing export button accessible name to `Export guest data` while the route contract expected account-export wording | Restored stable button name while still routing guest users through local guest export | Fixed; full E2E passed |
| Device simulation reported mobile bottom-nav overlap on metronome, score practice, More, and settings | `cd frontend && npm run simulate:devices` | New dense routes placed primary actions under the fixed mobile tab bar at small viewport heights | Added compact screen-scoped headers, mobile settings profile ordering, and smaller tiny-phone chrome | Fixed; device simulation passed |
| Mobile demo recording journey reached review page but guest-save proof text was hidden | `cd frontend && npm run e2e:local`, mobile Chromium/WebKit demo recording journey | A broad mobile CSS rule hid all page-header descriptions, including the session review guest-save evidence | Scoped description suppression only to dense metronome, score, settings, and More screens | Fixed; targeted mobile rerun and full E2E passed |
| Local implementation existed only in the dirty working tree after docs commit `cd37fee3...` | `git status --short --branch`, `git diff --stat`, `git ls-files --others --exclude-standard` on 2026-06-20 | Code, tests, screenshots, and Codex setup files were present locally but not committed to the pushed PR head | Preserved binary unstaged patch, empty staged patch, untracked file list/archive, checksums, and head record at `/tmp/brasstune-local-work-20260620-031847`; created branch pointer `backup/local-before-integration-20260620-031847` | Recoverable; implementation inventory and commit grouping in progress |
| Recovery worktree moved after the first safety bundle | Release-manager audit compared `/tmp/brasstune-local-work-20260620-031847/unstaged.patch` with the current diff | Backend auth-mode, dependency, docs, env, Render, and score-practice edits happened after the initial preservation snapshot | Created a fresh current-state bundle at `/tmp/brasstune-local-work-20260620-032819`; checksums and reverse patch check passed; created `backup/local-before-integration-20260620-032819` | Recoverable; use the newer backup for current recovery |
| Exact-SHA Vercel preview route smoke logged guest-mode `401` errors | `E2E_START_LOCAL_SERVERS=0 E2E_BASE_URL=https://brass-tune-7es12gogt-aryaswebsites.vercel.app E2E_VERCEL_SHARE_URL=... E2E_API_BASE_URL=https://brasstune.onrender.com E2E_WS_BASE_URL=wss://brasstune.onrender.com npm run e2e:hosted -- --project=chromium` against `72bb5a4` | Guest route visits caught auth failures but still triggered protected cloud fetches such as `/api/users/current`, `/api/sessions`, `/api/recommendations`, and ensemble endpoints, which Chrome logged as resource errors | Gated protected dashboard/session/analytics/progress/coach/ensemble/profile fetches on signed-in auth, and kept unauthenticated `/users/current` local-default lookup out of hosted Render fallbacks | Fixed locally; `npm test`, `npm run build`, and `CI=true npm run e2e:local` passed. Requires new exact-SHA preview rerun after push |

| Failure | Reproduction | Root Cause | Resolution | Current Status |
|---|---|---|---|---|
| GitHub Security / Gitleaks failed on PR #2 commit `a77a669...` | GitHub Actions Security run `27784019566`, Gitleaks step | `gitleaks/gitleaks-action@v2` could not read PR commits because workflow token lacked `pull-requests: read`; logs showed `Resource not accessible by integration`, not a leak finding | Added `permissions: contents: read, pull-requests: read` | Fixed historically; Security passed on PR head `91ca605...` |
| GitHub Frontend failed on PR #2 commit `a77a669...` | GitHub Actions Frontend run `27784019215`, Firefox Playwright recording journey | Recording journey waited on brittle state and server launcher emitted missing-venv fallback noise | Added venv-aware Playwright backend launcher, idempotent record/stop state, robust recording waits, mobile WebKit project, and artifact upload | Fixed historically; Frontend passed on PR head `91ca605...` |
| GitHub Frontend failed on commit `5604cc0` WebKit recording journeys | GitHub Actions Frontend run `27788619332`, WebKit and mobile WebKit recording journey | Backend logs showed `sqlite3.OperationalError: database is locked`; CI WebKit requests overlapped SQLite file writes long enough to hide the review link | Added SQLite `busy_timeout=30000`, WAL mode, and regression coverage for the engine pragmas | Fixed historically; latest CI repair adds an isolated temp SQLite DB for the Browser release journeys step |
| Local device simulation failed after copy/auth-shell changes | `cd frontend && npm run simulate:devices` | Simulator asserted floating tab bar visibility on auth routes, but auth shell intentionally hides app navigation | Updated simulator to skip mobile chrome assertion on auth routes | Fixed locally; device simulation passed |
| Hosted WebSocket handshake failed | Node `WebSocket('wss://brasstune.onrender.com/ws/pitch')` with ping | Render installed `uvicorn` without `websockets`/`wsproto`; Uvicorn logged `No supported WebSocket library detected` and returned `404` on upgrades | Replaced `uvicorn` with `uvicorn[standard]`, deployed exact commit `395a9d2` to Render with cleared cache, added hosted smoke script/test | Fixed: direct Node and `npm run smoke:hosted` passed |
| Native Settings account deletion UI smoke failed | `xcodebuild test ... BrassTuneAppUITests/testLaunchPracticeAndSettingsSurfaces` | On compact iPhone tab bars, Settings is under `More`; account lifecycle controls were also below tuner settings, making the deletion entry point brittle to discover in UI automation | Updated XCUITest to follow the `More` route and moved Settings Data/export/delete controls to the top | Fixed: focused XCUITest passed on iPhone Air simulator, result `/tmp/brasstune-dd-ui-handoff-fixed7/Logs/Test/Test-BrassTuneApp-2026.06.18_20-17-27--0500.xcresult` |
| Native UI runner intermittently failed before app launch | `xcodebuild test ... BrassTuneAppUITests/...` after repeated simulator runs | CoreSimulator returned `Busy` during XCTRunner preflight, before the app test body ran | Restarted CoreSimulator and used a fresh iPhone Air simulator | Environment issue cleared; final focused XCUITest passed |
| Protected Vercel preview Playwright route checks failed | `E2E_BASE_URL=https://brass-tune-9f0sicpxl-aryaswebsites.vercel.app ... npm run e2e:hosted -- --project=chromium` | Vercel Authentication returns `401` to local Playwright; connector fetch can access the deployment but browser automation lacks a reusable bypass | Added `E2E_VERCEL_SHARE_URL` route support and documented need for Vercel automation bypass | Blocked by Vercel preview auth configuration |
| Hosted strict legal route check failed before deploy | `E2E_STRICT_HOSTED_CONTENT=1 npm run e2e:hosted` against current production | Vercel production still serves the older deployment, so `/privacy` does not expose this branch's legal surface yet | Default hosted smoke now checks availability/connectivity; strict content is opt-in for post-deploy validation | Blocked until this branch is deployed |
| GitHub Swift failed on PR #2 commit `fc7ee5d` | Swift Action `Native app unit tests` step: `xcodebuild test ... -scheme BrassTuneApp ... -only-testing:BrassTuneAppTests` | The implicit Xcode scheme built both unit and UI test bundles during the unit-test step, and the unit test class used class-level `@MainActor` isolation that is brittle across Xcode 16.4/Xcode 26 toolchains. Local reproduction also showed Debug simulator builds could request an extra inactive arch and fail to import the local Swift package module. | Added explicit shared unit/UI schemes, moved actor isolation to individual unit tests, set Debug `ONLY_ACTIVE_ARCH=YES`, and kept the UI smoke on its own scheme. | Fixed historically; native unit tests passed before the later `d05fe773...` UI smoke failure |
| GitHub Frontend stuck on PR #2 commit `fc7ee5d` | Frontend Action `Browser release journeys` step | `npm run e2e:local` discovered all e2e specs including hosted-only smoke, produced heavier CI artifact output, and reused the default repo SQLite DB for all CI browser projects. | Scoped `e2e:local` to release journeys, added CI global timeout and blob reporter, set the Actions step to an isolated temp SQLite DB, and kept hosted smoke explicit. | Fixed; Frontend passed on PR head `91ca605...` |
| GitHub Swift failed on PR #2 commit `d05fe773...` | Swift Action `Native app UI smoke test` step after package, simulator boot, Debug build, and native unit tests passed | `PracticeView` read nested `NativeAudioEngine` state through `AppModel`, so SwiftUI did not observe recording-state changes. The UI smoke could tap the same stale start control twice and never reliably save `Demo take`; the flow also needed explicit UI-test state reset and a clean simulator install before the smoke. | Made practice tuner and microphone sections observe `NativeAudioEngine` directly, added stable `practice.recordingState`, added `UITEST_RESET_STATE`, cleaned the app install before UI smoke, and uploaded `.xcresult` on CI smoke failure. | Fixed; Swift passed on PR head `91ca605...` |
| GitHub Frontend failed on PR #2 commit `0f2e46a` | Frontend Action `Browser release journeys`, mobile WebKit `demo recording creates a reviewable session with playback surface` | Demo-mode practice stopped only after synthetic audio upload completed, so mobile WebKit could exceed the review-link wait even though the session was being created. The journey also waited only on visible text instead of the exact start/stop API responses. | In demo mode, stop the practice session before waiting on synthetic audio upload; clear the audio-recorder start promise for demo starts; bind the Playwright journey to the created session id and stop response. | Local targeted mobile WebKit journey passed, and local full `CI=true npm run e2e:local` passed `30 passed`; verify the next Frontend Action before merge |
