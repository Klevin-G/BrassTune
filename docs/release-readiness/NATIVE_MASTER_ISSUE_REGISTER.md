# Native Master Implementation Issue Register

> Local-only evidence registry. It contains no credentials or raw identity data,
> but user-home paths must be normalized before external publication.

Updated: 2026-08-06. Native baseline revision: `0c03bb54595567166a145a9a2640f0f946432e11`; native build `1.0.0 (3)` was archived from the preserved uncommitted worktree, exported, uploaded, processed, assigned to internal QA, selected for App Store version 1.0, installed on the wired iPhone through a byte-identical release-testing export, and submitted to App Review at the owner's direction. App Store Connect reports **Waiting for Review**; manual release remains selected and the app is not public. The separately reviewed backend repair is deployed at `1cd195ed75dee61420d497438f4a4b54c07be25f`, while the hosted frontend is `288d83091616acc0d906869bb6389721ac3a6017`. After the secure XCTest authorization was completed, the exact installed candidate passed the complete 20/20/20/20 plus 50-switch built-in-route matrix with screen recording off, microphone off, and microphone on; the final signature scan and device crash inventory found no equivalent failure.

This register tracks the attached native iOS implementation mission. It is not
an App Store readiness claim. Simulator, hosted, signed-archive, and physical
device evidence remain separate.

| ID | Severity | Surface | Status | Owner / disposition | Evidence or verification gate |
|---|---:|---|---|---|---|
| NATIVE-001 | P1 | First-launch Keychain | Implemented; simulator verified | Signing/Keychain lane | Signing and target entitlements now produce a simulator application identifier. A clean signing-enabled simulator launch showed no `-34018` or first-launch storage banner; physical-device and final-distribution identity remain separate gates. |
| NATIVE-002 | P1 | Primary navigation | Implemented; simulator exercised | Navigation lane | Five typed tabs read Practice / Tuner / Scales / Progress / More, use independent stacks, and disable broad tab-selection animation. The navigation UI scenario passed; the broader device/accessibility matrix remains pending. |
| NATIVE-003 | P1 | Microphone/audio state | Exact-candidate built-in-route matrix passed; accessory coverage open | Audio lane | The exact installed build-3 candidate passed 20 Tuner and 20 microphone Scale captures, background/foreground recovery, and 50 cross-feature switches under screen recording off, microphone off, and narration microphone on. The post-run device inventory added no BrassTune report and the original signature scan returned zero matches. Earlier real permission deny/grant/retry and `audiomxd` reset journeys also passed in the isolated `.dev` build. Lock/unlock completeness, unavailable accessory state, and Bluetooth/wired input routes remain required. |
| NATIVE-004 | P1 | Tuner UI | Exact-candidate repeated capture and sustained A440 passed; calibration open | Tuner UI lane | The exact build-3 candidate completed three 20-cycle Tuner matrices and a continuous A440 run with 40/40 stable half-second samples, written B4, 439.9–440.1 Hz, 0 cents, and 1,133.4 ms already-present-tone acquisition. Earlier real OS deny -> Settings grant -> retry passed without Ready or fabricated measurements. These are consumer-sine/display observations, not calibrated source-onset latency or real-brass validation; accessory interruption and full range acceptance remain open. |
| NATIVE-005 | P1 | Interactive warm-up | Implemented; focused simulator flow passed | Warm-up lane | Data-driven five-minute routine, 4/1/8 breathing phases/orb, Reduce Motion path, previous/next/skip, pause/resume/restart, explicit keep/discard exit, persistence, and deterministic tests are present. Start/pause/exit/resume passed in UI; background/finish/restart UI coverage remains P2. |
| NATIVE-006 | P1 | Scales/music theory | Implemented; Core/UI gates passed | Music-domain + navigation lanes | Core covers 12 roots across major, natural/harmonic/melodic minor and chromatic, one/two/three octaves and directions. Practical-range validation fails closed; default trumpet combinations may be unavailable. The exhaustive Core matrix covers 5,940 profile/configuration combinations (11,880 policy calls); visual timing passed in UI and written-octave rejection is unit-tested. This is internal contract evidence, not independent musical/editorial validation. |
| NATIVE-007 | P1 | Instrument model | Implemented; editorial review pending | Music-domain lane | Central Core registry feeds native range/transposition and shared fixture bounds now match backend/frontend consumers. Advanced-player range choices and pedagogy metadata still need brass-domain editorial review. |
| NATIVE-008 | P1 | Progress truthfulness | Implemented; simulator flow passed | Progress/persistence lane | Activity-aware records exclude non-pitch work from tuning metrics, meaningful-duration gates reject accidental taps, and denied-mic recommendations expose a warm-up CTA; the saved-history/denied-mic UI scenario passed. |
| NATIVE-009 | P1 | Practice session/export schema | Implemented; unit suite passed | Progress/persistence lane | Activity/completion/stable identity, migration defaults, deterministic schema-version-1 JSON export, score-preserving history clearing, and typed visual-scale configuration persistence are covered in the current 240-test native unit suite. |
| NATIVE-010 | P1 | Native Classes | Release-disabled | Product decision | Native sessions never sync to server class aggregates, and moderation/minor/legal gates remain open. No release-visible Classes entry point or class-code promise. |
| NATIVE-011 | P1 | Offline packs | Release-disabled | Product decision | Current implementation is two bundled routines, not a download lifecycle. It is hidden from Release until size/progress/cancel/retry/integrity/storage/licensing exist. |
| NATIVE-012 | P1 | Auth/account lifecycle | Native Google/Apple and Apple web lifecycles passed; private-audio/account lifecycle remains open | Account UX + external verification | Unsupported backup/Class claims are removed. Guest-origin credentials are not persisted until an explicit merge/separate choice; merge is deterministic and file-backed data fails closed. On 2026-08-04, isolated physical `.dev` Google R7 and Apple R8 each passed real provider completion, callback, generic signed-in state, cold restore, sign-out, and signed-out relaunch; a current attended Apple repeat passed the same sequence `1/1` in 203.336 seconds. Production web Google sign-in/session restore/sign-out passed, and one synthetic recording persisted. The same-origin proxy and signed-in delete control are deployed at `1cd195ed75dee61420d497438f4a4b54c07be25f`; hosted `45/5` and signed-out playback/export `401` checks pass. Apple web Services ID/domain/callback/key/client-secret configuration persists, and a fresh Safari Apple authorization completed callback, created an Apple social-auth identity, restored after reload, signed out, and remained signed out after another reload. Owner playback/export/delete, final object absence, account deletion, and live cross-user denial remain open. Publishable evidence: `LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`. |
| NATIVE-013 | P1 | Release identity/signing | Build 3 archive, upload, processing, and exact-payload device exercise passed | Signing lane + owner | The exact `1.0.0 (3)` r2 archive passed strict signature, production bundle ID, `get-task-allow=false`, `beta-reports-active=true`, Sign in with Apple `Default`, privacy-manifest, and matching app/dSYM UUID checks. App Store Connect accepted and processed IPA SHA-256 `995eac30b0be666e1768c4de0fee081e33f35208ff005beb8b3991d02ce19076`; build 3 is Ready to Submit, assigned to internal QA, and selected for version 1.0. The archive was re-exported with the team Ad Hoc profile; after stripping signatures its Mach-O is byte-identical to the App Store archive, and that installed payload passed the exact-candidate physical audio matrix. This closes signing/upload and the exercised audio payload mechanics, not legal submission approval. Evidence: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/AppStoreCandidate-2026-08-06-Build3`. |
| NATIVE-014 | P1 | Physical audio validation | Exact build-3 crash families closed for built-in route; broader route/timing gate open | Owner/device | On the wired iPhone 15 Pro Max / iOS 26.4.1, the exact installed build-3 candidate passed the full 20 Tuner / 20 microphone Scale / 20 Metronome / 20 Drone-reference tone / background-recovery / 50-switch matrix three times: screen recording off, microphone off, and narration microphone on. Sustained A440 passed 40/40 display samples, and three additional 180-second 120-BPM Metronome runs passed. Final execution-log scans contain none of the three original signatures, and the post-run device inventory added no BrassTune report. Earlier `.dev` evidence separately passed permission recovery, real media-services reset, and instrumented AirPlay selection. Bluetooth HFP/A2DP, wired accessories, production audible AirPlay, calibrated timing/latency, and complete accessibility remain open. Evidence: `NATIVE_PHYSICAL_RELEASE_REPORT_2026-08-06.md` and the build-3 evidence root. |
| NATIVE-015 | P1 | App Store materials | Submitted; Waiting for Review | Release lead + owner/legal | Five opaque `1320 x 2868` iPhone screenshots and two opaque `2064 x 2752` iPad screenshots are uploaded. Product copy, subtitle, keywords, URLs, review contact/notes, Music/Education categories, 4+ age rating, no-third-party-content declaration, free worldwide pricing, 175-region availability, iPhone/iPad-only storefront scope, manual release, build 3 selection, and the published App Privacy disclosure are saved. On 2026-08-06, the owner confirmed the DSA non-trader declaration while retaining EU distribution; App Store Connect reports Digital Services Act Active for all 27 EU countries/regions and all current regulatory requirements complete. The owner directed submission with the documented P1 validation gaps still open; App Store Connect now reports Waiting for Review. Accessibility nutrition labels remain conservatively undeclared. The app is not released and `Release/BrassTune-v1.0/` remains absent. |
| NATIVE-016 | P1 | Metronome timing | Exact-candidate repeated/screen-recorder/sustained runs pass; audible metric fails closed | Audio lane | Build 3 passed 20 Metronome cycles and cross-feature stress in all three recording conditions plus three 180-second sustained 120-BPM runs without the original signature. A SoX/CoreAudio workaround produced a continuous final 219.52-second 48 kHz/24-bit capture, but even at maximum app/device volume the detector resolved only 210/356 expected pulse bins at its lowest threshold and retained 834 extra candidates. The recording is preserved and the numerical jitter result is rejected for insufficient acoustic signal-to-noise. Independent calibration or a direct digital capture remains required. |
| NATIVE-017 | P1 | Sheet music | Implemented; partial real physical workflow pass | Content lane | Unit contracts pass and the 2026-08-01 source pass added a focused import-error target, a non-color page-selection checkmark/selected trait, and 44-point viewer pager targets with spoken labels. Real Photos PNG and iCloud Files PNG journeys each passed import, app-local persistence across termination/relaunch, and confirmed deletion without altering the original provider asset. The Photos journey also proved delete-cancel. Share/export presentation, PDF/JPEG/HEIC detection, corrupt-file recovery, and other providers remain open. Evidence: `physical-photos-import-persistence-delete-r2.xcresult` and `physical-files-import-persistence-delete-r2.xcresult`. |
| NATIVE-018 | P1 | Accessibility matrix | Physical structure partial plus current simulator substitutes; human/device gates open | Independent QA | Two genuine contrast defects were repaired; strict physical Light/Dark and physical maximum Dynamic Type + RTL passed, while the raw iOS contrast audit remains non-green with independently adjudicated false positives. New current-source simulator substitutes passed Increase Contrast + accessibility XXXL/RTL/strict audit `2/2`, system Reduce Motion warm-up/Metronome `2/2`, iPhone orientation `1/1`, and iPad navigation/orientation `2/2`. VoiceOver is owner-reported passed. Voice Control, Differentiate Without Color perception, alert-focus restoration, physical system-toggle review, physical iPad, and uncoached usability remain open. |
| NATIVE-019 | P1 | Privacy/legal | Native presentation, archive manifest, App Store privacy disclosure, and current DSA declaration verified | Owner/legal | Public privacy, terms, and support URLs return HTTP 200; the focused legal UI test passes `1/1`. The uploaded archive contains a valid privacy manifest declaring UserDefaults and System Boot Time required-reason API use. App Store privacy is published for linked Email Address, User ID, and Other User Content used only for App Functionality, with no tracking. On 2026-08-06, the owner confirmed non-trader status while retaining EU distribution; App Store Connect reports the DSA requirement Active for all 27 EU countries/regions. Reassess before future commercialization. |
| NATIVE-020 | P1 | Updated UI automation | Current full physical plan passed; coverage reconciliation open | Independent QA | Developer Tools security is enabled and the repaired Release `.dev` runner completed the full standard physical suite at 20/20 with zero failures/skips on 2026-08-03. It covers onboarding, typed navigation, guided all-notes practice, score viewer/rename/delete-cancel, background recovery, legal/settings/export scope, account/provider presentation, destructive confirmations, maximum-Dynamic-Type fixture, and RTL fixture. Evidence: `physical/ui/release-dev-standard-20-after-accessibility-repair.xcresult`. This remains fixture-based UI evidence, not complete assistive-technology, real-provider auth, or real Files/Photos coverage. |
| NATIVE-021 | P1 | Guest-upgrade relaunch | Implemented; unit suite passed | Auth lane | Credentials/auth commit now occur only after target preparation and a synchronous merged-snapshot write. Corrupt target, account-write, and Keychain failure paths restore guest state; success and failure relaunch regressions pass. |
| NATIVE-022 | P1 | Realtime callback | Build-2 trap family closed on exact build 3 for built-in input; unavailable routes open | Audio lane | The exact installed candidate completed three sets of 20 Tuner and 20 microphone-Scale captures plus 50 switches with zero failures and no `dispatch_assert_queue` signature or new BrassTune report. Earlier `.dev` diagnostics recorded 30/30 Scale completions, 60 accepted frames, no missing/empty completion, zero drop events, and recovery after a real media-services reset. Bluetooth HFP and wired microphone routes remain unavailable. |
| NATIVE-023 | P0 | Production Supabase identity | Identity/configuration, native Google/Apple lifecycle, Apple web lifecycle, and proxy deployment passed; private-audio acceptance remains open | Owner + release lead | Release settings, repository link/config, signed app, Render backend, and live public auth endpoint resolve to the same production project; Google and Apple are enabled and the eight-entry redirect policy remains reconciled. Real physical `.dev` Google R7, Apple R8, and the current attended Apple repeat callback/restore/sign-out lifecycles passed, and production web Google sign-in/restore/sign-out plus one synthetic upload were exercised. PR 26 deployed the reviewed proxy/delete repair at `1cd195ed75dee61420d497438f4a4b54c07be25f`; PR 27 deployed enabled Apple/Google/email entry points at `288d83091616acc0d906869bb6389721ac3a6017`. Apple Developer persists the web Services ID with Sign in with Apple, production primary App ID, domain, callback, and dedicated key; the Supabase Apple provider persists the ordered web/production/`.dev` client IDs plus masked client secret. A fresh Safari Apple flow completed provider authorization, callback, signed-in session restore, sign-out, and signed-out reload; the dashboard classified the new identity as Apple-backed. Live owner read/delete, object absence, account deletion, and cross-user acceptance remain open. Publishable evidence: `LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`. |
| NATIVE-024 | P1 | Persistence migration write-back | Fixed in current worktree; targeted and full simulator tests pass | Persistence follow-up | The restored-snapshot writeback token now defers migration/quarantine persistence while namespace identity is restoring, consumes it only after guest/account access becomes writable, and re-arms after a failed asynchronous write without a retry loop. Three explicit disk/second-relaunch regressions cover legacy metronome migration, account restoration after one failed write, and shipping fixture quarantine; targeted `3/3` and the full native `240/240` pass. Evidence: `simulator/BrassTune-NATIVE024-20260803.xcresult`. This source is still in the dirty uncommitted native worktree, so exact-candidate reproducibility remains open. |
| NATIVE-025 | P2 | Audio regression coverage | Deterministic lifecycle seams, TSan, and real media reset pass; route-selection survival is partial | Audio follow-up | Deterministic overflow/fail-closed, drain/discard, teardown, stale-publication, drop-event, metronome-completion, media-reset, and fresh-activation regressions cover the pipeline. Normal simulator `240/240` and TSan `239/239` pass. A real media reset passed. Two `PHYSICAL_RELEASE` AirPlay-selection tests proved UI/audio-owner survival only; they did not verify audible output or production route identity. |
| NATIVE-026 | P0 | Build-2 microphone crashes | Closed on exact build 3 for tested built-in route and original screen-recorder conditions | Audio + physical QA | Build 3 passed three complete matrices with 20 Tuner, 20 Scale, and 50 switches each: recording off, recording microphone off, and narration microphone on. The post-run inventory contains no new BrassTune report and scans contain no `dispatch_assert_queue` or `InstallTapOnNode` signature. Bluetooth HFP/wired input remain broader P1 route gaps. |
| NATIVE-027 | P0 | Build-2 metronome crash | Closed on exact build 3 for tested built-in route and original screen-recorder conditions | Audio + physical QA | Build 3 passed three complete matrices with 20 Metronome cycles and 50-switch stress each. No `ScheduleBuffer`/`NativeMetronomeOutput` signature or new BrassTune report appeared. The AirPlay route-picker run remains instrumented survival, not production audible-route closure; jitter and accessory routes are broader P1 gates. |
| NATIVE-028 | P1 | Reference tone and audible cues | Physical built-in lifecycle passed; acoustic/route acceptance open | Audio + physical QA | Rebuilding a fresh output graph at the capture-to-tone handoff repaired 60 reproducible `kAUStartIO` failures. Later Debug/Release-equivalent built-in-route runs completed repeated reference-tone/Drone starts with zero failures. The route-picker seam survived HomePod selection but did not independently prove audible output. Exact level, silent-switch policy, haptics, Bluetooth, wired, and production AirPlay remain unmeasured. |
| NATIVE-029 | P0 | Exposed Supabase secret rotation | Closed provider-side on 2026-08-03 | Owner + release lead | The owner deleted the previously exposed secret in Supabase. The replacement was entered only into Render's server-side `SUPABASE_SECRET_KEY` variable and an environment-only deploy reached live as `dep-d9ojmitbedkc73d239dg`. Direct read-only requests to the matching Supabase project returned HTTP 200 for both Admin and Storage, Render readiness returned HTTP 200 with all checks green, and hosted smoke passed 8/8. The key value is intentionally absent from repository files and evidence. Manual source/history/archive scans continue to show no credential-shaped secret or private key in inspected public-client artifacts. |
| NATIVE-039 | P0 | Modern Supabase secret-key request headers | Header and proxy repairs deployed; authenticated lifecycle acceptance remains open | Backend + release lead | Supabase modern secret keys are no longer sent as bearer JWTs by identity deletion or audio Storage. Server calls send the secret only as `apikey`, while user validation/global sign-out retain the user's access token in `Authorization`. PR 26 merged as `1cd195ed75dee61420d497438f4a4b54c07be25f`; Render `dep-d9p5j97avr4c73amdkjg` serves that revision. Exact candidate backend `305 passed / 11 skipped`, focused proxy/resource/header/delete checks, hosted `45/5`, and signed-out playback/export `401` pass. This does not close signed-in read/delete, cross-user denial, Storage absence, or account deletion. Evidence: `LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`. |
| NATIVE-030 | P1 | Physical accessibility and usability | Partial physical pass plus stronger simulator substitutes; broader matrix open | Owner + independent participants | Physical navigation/onboarding/warm-up/legal/settings/scale/score/Dynamic-Type/RTL structure passed; VoiceOver is owner-reported passed. New simulator substitutes passed Increase Contrast + accessibility XXXL/RTL/strict Light/Dark `2/2`, system Reduce Motion warm-up/Metronome `2/2`, iPhone orientation `1/1`, and iPad current-source navigation/orientation `2/2`. Real Photos/iCloud Files PNG journeys remain historical. Voice Control, Differentiate Without Color perception, alert-focus restoration, physical system settings, physical iPad, additional formats/share/export, and uncoached usability remain open. |
| NATIVE-031 | P1 | Capture-to-output graph handoff | Repaired; physical Debug and Release-equivalent pass | Audio lane | A deterministic physical run reproduced 60 `kAUStartIO` failures when reference tone/Drone attempted to reuse the capture-used AVAudioEngine graph under a playback session. `startTone` now rebuilds a fresh output graph after capture teardown. Exact reruns passed with zero failures in both physical configurations. |
| NATIVE-032 | P1 | Main-thread audio transition hangs | Passed for one bound current-source physical observation; repeatability limits remain | Audio/performance lane | A new Release-optimized `.dev` Time Profiler observation on the iPhone 15 Pro Max ran 50.836 seconds, recorded 6,400 samples, zero potential hangs at the template's >=250 ms threshold, zero hang-risk rows, and Nominal thermal state. Its bound `AudioSessionDeactivate` signpost completed in 11.269 ms. This supersedes the older 12-stall trace for this exact bound workload, not as a population-wide conclusion. Evidence: `physical/current-source-performance-2026-08-03/README.md`, `metrics.json`, and `traces/time-profiler-current.trace`; app-input manifest `f0021dd7c4184d089a323051c1a7373e28a78bfc72e30307bdb10db99950c4f1`, binary UUID `237789E0-032B-3781-AF8B-68003568A0F8`. |
| NATIVE-033 | P1 | Release-equivalent physical profiling | Passed for one bound current-source physical observation; release population limits remain | Performance lane | The bound Release-optimized `.dev` workload completed 20 Tuner, 20 Scale, 20 Metronome, 20 reference-tone, 20 Drone, and 50 switches with 30/30 Scale completions, 60 accepted frames, zero dropped-input events, and zero failures. Activity Monitor over 50.908 s measured CPU time/wall 12.271%; 41 samples mean 14.744%, p95 33.073%, peak 40.163%; real memory 60.875-67.953 MiB; threads peak 18; Nominal thermal state. Post-install/warm terminated launches reached foreground active in 643.064/507.547 ms. No new crash report appeared. This is one wired phone, one workload, no idle baseline/reboot-cold-launch/battery-drain confidence interval, and no clean-SHA archive; do not generalize it beyond that scope. Evidence: `physical/current-source-performance-2026-08-03/`. |
| NATIVE-034 | P1 | 2026-08-02/04 UI/auth/scales/score/streak pass | Simulator and built-in crash-family physical pass; provider lifecycles passed | Native + owner/provider lanes | Current uncommitted source: Swift Core `12/12`, simulator units `240/240`, preserved UI `22/22`, localization clean. The artifact-bound August 2 Release `.dev` Runner closed A/B/C for the exercised built-in route but is not exact-current-candidate evidence. Authorized Google R7/Apple R8 lifecycles passed; VoiceOver is owner-reported. Production TestFlight remained untouched and no archive/upload/release folder was created. |
| NATIVE-035 | P1 | Physical focused UI automation | Exact-source Release Runner passed after one infrastructure retry | Native QA + signing lane | The first new-phone attempt failed before test execution with `Timed out while enabling automation mode`; it is preserved as infrastructure evidence. Retry 1 then passed `1/1` in 492.429 seconds on the exact final Release source, exercising 20 Tuner, 20 microphone Scale, 20 Metronome, 20 reference tone, 20 Drone, and 50 cross-feature switches with zero failures and zero detected Tuner input-buffer drop events. Evidence: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/ReleaseCandidateAudit-2026-08-02/NewPhone-i15PM/physical-release-audio-crash-family-runner-final-source-retry1.xcresult`; this is preserved August 2 audit evidence, not inside the August 3 report root. |
| NATIVE-036 | P1 | Final Release-equivalent device build | Build/install/Runner passed; broader release blocked | Signing + device lane | The final Release-optimized `com.aryasalem.BrassTune.dev` build installed on the iPhone 15 Pro Max beside untouched production build 2 and passed the exact-source physical crash-family Runner. After profiling, the non-instrumented Release `.dev` build was restored. The production bundle remained version `1.0.0 (2)`. This is not provider-flow, archive, upload, or full App Store readiness proof. |
| NATIVE-037 | P0 | Production Sign in with Apple provisioning | Closed through accepted build-3 upload | Owner/signing lane | The explicit App Store profile matches `com.aryasalem.BrassTune`, contains Sign in with Apple `Default`, has `get-task-allow=false` and `beta-reports-active=true`, and binds to the installed Apple Distribution identity. The r2 archive signature, entitlements, app/dSYM UUID, export, upload, and App Store processing all passed. |
| NATIVE-038 | P1 | 2026-08-03/04 physical release-candidate closure | Automated physical, native-provider, and production build-only signing scope passed; release blocked | Release integration | On iPhone 15 Pro Max / iOS 26.4.1, the Release `.dev` build passed the complete 20-test standard UI plan, current-source audio self-test 20/20/20/20/20 plus 50 switches with zero failures/drop events, and three screen-recording conditions. The bound physical performance observation again passed the full self-test, had zero >=250 ms Time Profiler stalls, and captured CPU/memory/launch values under exact manifest/binary binding; that manifest predates the later display-only AppRootView/UI-test patch and does not bind the entire final dirty worktree. Simulator evidence includes normal `240/240` and TSan `239/239`, final stable-and-estimating UI `22/22`, exact iPad/iPhone navigation `1/1` each, and physical maximum-Dynamic-Type/RTL `1/1`. Physical auth-provider initiation passed `3/3`, and superseding authorized `.dev` lifecycles passed Google R7 and Apple R8 `1/1` each through callback, cold restore, sign-out, and signed-out relaunch. Fresh isolated local Playwright passed 443/7 skipped/0 failed (450 total, 7.3m). App Store readiness remains blocked by signed-in modern-secret Storage/account lifecycle verification, unavailable routes/hardware, fail-closed physical metronome jitter, uncalibrated music/latency, incomplete assistive-technology/usability coverage, legal/privacy gates, synthetic tuba low-register robustness risk, and the dirty uncommitted native worktree. Production TestFlight build 2 remained installed and untouched; no archive/upload/release folder was created. |
| NATIVE-040 | P1 | Tuner stable-display gating | Display fix and final simulator UI validation passed; physical display validation provisional | Native display lane | The narrow display-path repair now has focused coverage and the complete final native UI suite passes `22/22`, zero failures/skips, on iPhone 17 Pro / iOS 26.5; it includes estimating-hidden and stable-visible tests. Evidence: `simulator/current-ui-continuation-2026-08-04/BrassTuneAppUISmoke-post-stable-and-estimating-gates.xcresult`; start `1785824150.84`, finish `1785824757.484`; Info.plist SHA-256 `855af0b7d44d5dd0c2bc238cbc26390b7c7d80c500d21487114d1ba8a30b4074`; log SHA-256 `0bc18f5fc7823c2d7e3fd2bf9f187aa0f2e4cf278631cff09399c1b9f6cfb7c2`. This closes the simulator display-gating regression, not a physical tuner-display or calibrated music gate. Separately, the expanded deterministic synthetic sweep passed aggregate gates (13,920 steady cases, 132 onsets) but found one reproducible low-register tuba synthetic nearest-note flip and tuba p95 8.9947 cents versus the shared 8-cent gate; all three implementations reproduce it below recording confidence. |
| NATIVE-041 | P2 | Shared leading-icon button inset | Repaired and simulator-validated; physical spot-check open | Native UI lane | The shared primary and secondary button styles now provide tokenized horizontal insets, fixing the edge-adjacent Warm-up, Guided Practice, and equivalent leading-label controls. Compact iPhone SE QA found the global inset made the three metronome tempo controls wrap poorly; those intentionally dense controls now use the smaller `BTSpacing.xs` override while all standard controls retain `BTSpacing.lg`. iPhone 17 Pro focused UI passed `4/4`. On iPhone SE / iOS 26.5, the maximum-Dynamic-Type/RTL primary-target and metronome quick-tool scenarios passed `2/2`; two existing below-fold assumptions were hardened with `bringIntoView`, and their exact rerun passed `2/2`. Independent re-review found no residual P0-P2 issue. This is simulator evidence only; no production bundle, archive, upload, or physical-device claim was created. |
| NATIVE-042 | P1 | App Store submission preflight | Submitted; Waiting for Review; manual release pending | Release integration + owner/legal | Build `1.0.0 (3)` is uploaded, processed, assigned to BrassTune Internal QA, selected for version 1.0, and submitted to App Review. Screenshots, copy, review notes/contact, manual release, categories, 4+ rating, content rights, pricing, privacy disclosure, and the owner-confirmed DSA non-trader declaration are saved; App Store Connect reports DSA Active and Waiting for Review. The exact installed archive payload passed 20 launch cycles, three complete audio stress matrices, sustained A440, signature scan, and post-run crash inventory. Broader P1 acceptance gaps remain documented and were not relabeled as passes. Evidence: `NATIVE_PHYSICAL_RELEASE_REPORT_2026-08-06.md` and the build-3 evidence root. |
| NATIVE-043 | P1 | Build-3 exact-candidate physical closure | Audio crash-family gate passed for tested built-in route; broader acceptance open | Native QA + release integration | The release-testing export's unsigned Mach-O is byte-identical to the uploaded r2 archive, and the wired iPhone reports production bundle `1.0.0 (3)`. After secure automation authorization, `TestFlightInspect-r7.xcresult` passed and the exact installed bundle completed three full 20 Tuner / 20 microphone Scale / 20 Metronome / 20 Drone-reference tone / 50-switch matrices under screen recording off, microphone off, and narration microphone on. Sustained A440 passed 40/40 samples. The final scan found zero original signatures and the post-run device inventory found zero new BrassTune reports. A/B/C are closed for this tested route; accessory, calibrated timing, performance, and full usability remain P1. |

## Current evidence

### 2026-08-04 live auth/storage evidence (supersedes initiation-only status where applicable)

The sanitized publishable record is
[`LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`](LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md).
On the wired iPhone 15 Pro Max / iOS 26.4.1 / Xcode 26.6 lane, Google native
physical R7 and Apple native physical R8 each passed `1/1`: real provider
completion, callback, generic signed-in state, cold restore, sign-out, and
signed-out relaunch. Apple R7 completed authorization but exposed a
false-negative Settings-versus-Practice test assumption; the narrow state-probe
repair, authorized-session cleanup, rebuilt signed-out preflight, and full R8
rerun all passed. The TestFlight production app `1.0.0 (2)` remained
installed and its installation path unchanged; the optimized Release `.dev` app
was restored and launched after the UI runner.

At historical backend SHA `eb4dac6749fb7fd954d79ef7e90a7687a8e428c7`,
an authorized ordinary-student Google session restored and one synthetic Demo
WAV uploaded with persisted metadata, but playback/export failed with the
privacy-safe redirect probe. The subsequent backend SHA
`1cd195ed75dee61420d497438f4a4b54c07be25f` and frontend SHA
`288d83091616acc0d906869bb6389721ac3a6017` passed hosted smoke `45` with `5`
skipped; signed-out playback/export returned `401`. No authenticated owner
read/delete, cross-user denial, account deletion, or final Storage absence is
yet proved against the repaired deployment.

The final Vercel production build at `288d83091616acc0d906869bb6389721ac3a6017`
exposes enabled Apple, Google, and email entry points across all five hosted
browser projects. Apple Developer web Sign in with Apple and the production
Supabase Apple provider are now configured; fresh Safari initiation reaches
Apple's real consent sheet instead of the former missing-secret HTTP `400`.
At that earlier checkpoint the attended web callback was incomplete. It was
superseded by the later owner-authorized Safari run in
`LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`, which completed authorization,
callback, restore, sign-out, and signed-out reload. Native Apple R8 remains
separately passed. Current physical `.dev` evidence adds provider discovery
`1/1`, Apple native consent `2/2`, and Google consent `1/1` on the wired phone.

The later owner-authorized configuration pass changed Apple Developer web Sign
in with Apple and the production Supabase Auth Apple provider only. It did not
change database schema, tables/rows, Storage, redirect policy, or RLS/grants.
Earlier authorized sign-in/upload created ordinary production Auth, session,
application, and Storage test data. One synthetic recording remains because the
authenticated deletion has not been accepted against the deployed control. The
non-secret GitHub and Vercel Production Apple build flags were set to `true`; a
full Vercel rebuild was deployed. The raw native result is private/quarantined;
use only the sanitized Markdown record above. Outcome remains **BLOCKED**.

- Exact candidate frontend context: `269` passed; focused deletion `30/30`,
  independent focused audit `34/34`, guest-audio Playwright `20/20`, and
  TypeScript/build passed. Exact candidate backend is `305` passed / `11`
  skipped, with proxy/resource/header/delete focused matrices passed.
- XcodeBuildMCP simulator build passed. Its wrapper test call exceeded the
  600-second wait window, while the retained log later recorded `22/22` passed,
  zero failures, 598.950 test seconds / 609.114 elapsed. Device simulation
  passed all 13 configured viewports.
- Swift Core remains `12/12`; exact signed generic-device `PHYSICAL_RELEASE`
  runner compile passed. Final crash-log listing found no new BrassTune report
  after 12:16 PM or from the current 15:xx runs.

- Xcode 26.6 and iOS 26.5 SDK are installed.
- Current Swift Core passes `12/12` tests. The earlier `9/9`, `10/10`, and 3,960-configuration records are historical evidence; the current three-octave matrix covers 5,940 combinations and 11,880 policy calls.
- Current native Debug and Release simulator builds pass on iPhone 17 Pro; current Release simulator builds pass on iPad Pro 13-inch (M5). A generic unsigned `iphoneos` Release compile also passes.
- Current simulator native tests pass `240/240`; Thread Sanitizer remains `239/239` with zero failures/skips and no sanitizer report. The later auth-probe isolation regression is not audio logic. The earlier `201/201`, `219/219`, and `237/237` records are historical evidence.
- Current merged targeted UI suite passes `7/7` with zero failures/skips at `/tmp/BrassTuneFinalMergedUI-20260802.xcresult`.
- The prior targeted `6/6` and separate guided-background `1/1` at `/tmp/BrassTune-GuidedBackgroundUI-20260802-run5.xcresult` are precursor evidence. Earlier focused audio/route/UX results at `/tmp/BrassTune-manual-targeted-20260728-p1-source.xcresult`, `/tmp/BrassTune-manual-route-state-20260728.xcresult`, and `/tmp/BrassTune-manual-ui-targeted-20260728-current-r3.xcresult` are historical evidence.
- The earlier full UI plan `15/15` result with a terminating summary (`/tmp/BrassTune-manual-full-ui-20260728-final.xcresult`) is historical evidence, not the current targeted UI count.
- Current localization covers `844` static keys / `980` catalog entries across 12 locales with all automated checks clean. Human linguistic review is not recorded.
- Exact deployment-candidate backend passed `305` with `11` PostgreSQL-only skips; exact deployment-candidate frontend passed `269`. The separate preserved dirty-worktree frontend result is `275/275`.
- The modern-secret header repair was first deployed at `eb4dac6749fb7fd954d79ef7e90a7687a8e428c7` and is carried forward in current backend SHA `1cd195ed75dee61420d497438f4a4b54c07be25f`; native Google R7 and Apple R8 remain passed. Authenticated Storage/account lifecycle and RLS acceptance remain external gates.
- The preserved signed build-2 archive and all five TestFlight crash logs share UUID `1B41F25F-F15B-3C96-9FEB-C9EEB4F28BBF`. Build 2 is evidence, not a candidate.
- Restart evidence is preserved under `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/PhysicalClosureRestart-2026-08-01-015053`. The final optimized Debug and isolated Release-equivalent runs each report Tuner `20`, microphone Scale `20`, Metronome `20`, reference tone `20`, Drone `20`, feature switches `50`, detected Tuner input-buffer drop events `0`, and failures `0`.
- Two additional Control Center screen-recorder Debug runs passed the focused physical matrix: narration microphone off in 501.462 seconds and narration microphone on in 501.617 seconds. Each completed Tuner `20`, microphone Scale `20`, Metronome `20`, reference tone `20`, and primary-tab navigations `50` with zero failures and no new crash report. The tab loop did not start another 50 audio features. Full physical MP4s, option/active/stop screenshots, structured results, postflight inventories, and the exact tested Debug binary are preserved in `Continuation-DgxbMK`.
- The physical Debug Instruments set contains Time Profiler, Allocations, Power Profiler, Audio System Trace, and Hangs traces. Audio HAL/client point-of-interest tables are empty, all exported I/O-cycle load buckets are `Normal`, thermal state remained `Nominal`, and Hangs found 12 microhangs. These are Debug observations even though the app target compiled with `-O`.
- The earlier Release-equivalent trace set contains two Time Profiler/Hangs runs plus Activity Monitor, Power Profiler, App Launch, and Audio System Trace. Those precursor Time Profiler runs reproduced one capture-start Hang (931.859 ms and 934.836 ms), then a later trace with 12 potential hangs from 251.13 to 335.60 ms. A newer bound current-source Release-optimized trace supersedes those values for its workload: 50.836 seconds, 6,400 samples, zero >=250 ms potential hangs, zero hang-risk rows, and Nominal thermal state. The paired 50.908-second Activity Monitor run reports CPU time/wall 12.271%, mean CPU 14.744%, p95 33.073%, real memory 60.875-67.953 MiB, and 18 threads peak. This is one device/run, not battery-drain or clean-SHA population proof.
- The exact final continuation artifact has bundle ID `com.aryasalem.BrassTune.dev`, UUID `8490C742-E282-3B91-A593-88F3E00A9D39` for both binary and dSYM, and binary SHA-256 `1e1bd03fdb388fe4c99a94a17d0f0347dd398155a3bd8c7ab449a6a09ad061d5`. It was not archived or uploaded.
- Current pass: Swift Core `12/12`, simulator native tests `240/240`, Thread Sanitizer `239/239`, preserved stable-and-estimating UI `22/22`, deterministic OAuth recheck `3/3`, merged targeted UI `7/7`, physical maximum-Dynamic-Type/RTL `1/1`, and localization clean. New simulator substitutes add Increase Contrast/accessibility XXXL `2/2`, system Reduce Motion `2/2`, iPhone orientation `1/1`, and iPad current-source navigation/orientation `2/2`. The artifact-bound August 2 physical Release-equivalent Runner closes the three original audio crash families for the exercised built-in route but is not exact-current-candidate or complete release evidence.
- Final installed-app enumeration still shows both `com.aryasalem.BrassTune` `1.0.0 (2)` and `com.aryasalem.BrassTune.dev` `1.0.0 (2)`. The production TestFlight app was not replaced or uninstalled.
- Current physical preflight identified a wired iPhone 15 Pro Max (`iPhone16,2`) on iOS 26.4.1 (`23E254`), connected, trusted, unlocked, and in Developer Mode with Xcode 26.6 (`17F113`). The earlier iPhone 17 Pro Max/iOS 26.6 record remains historical evidence.
- The signed Debug `.dev` bundle (`com.aryasalem.BrassTune.dev`, `1.0.0 (2)`) was provisioned and installed beside the untouched TestFlight production bundle (`com.aryasalem.BrassTune`). The production app was not replaced or uninstalled.
- Physical results are preserved under `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/PhysicalClosure-2026-07-28-2209`: focused metronome `1/1` passed, the remaining UI plan `14/14` passed, and the real microphone stress result failed at tuner cycle 12 with a recoverable `Microphone interrupted` state after 11 successful cycles. XCTest screen recordings, UI hierarchies, logs, and result bundles are included there.
- A physical app-hosted unit-seam attempt did not run tests because the test host was missing the `BrassTuneCore` package-product framework at launch. This is a test-host packaging failure, not product crash-family closure evidence.
- Developer Tools security is enabled. The rebuilt current-source Release `.dev` standard suite passed 20/20 with zero failures/skips, and the Release self-test passed all 20/20/20/20/20 feature cycles plus 50 switches with zero failures and zero detected Tuner input-buffer drop events. A later compile-gated Debug `.dev` run added Scale diagnostic propagation and passed 20 Scale cycles plus 10 Scale transitions with 30/30 completions, 60 accepted frames, no missing/zero-frame completion, zero drop events, and zero failures. Production Sign in with Apple provisioning, provider-side secret rotation, live provider discovery, native Google/Apple provider lifecycles, and exact backend source deployment pass; the raw contrast findings were independently adjudicated as false positives. Unavailable hardware routes, scientifically valid jitter, calibrated music/latency, complete assistive-technology/usability coverage, and signed-in Storage/account-lifecycle acceptance remain blocked.
- No next-build archive, validation, upload, complete physical-device closure matrix, accessibility pass, usability pass, complete performance pass, or App Store Connect completion is claimed.

### 2026-08-05 native-only USB continuation

The preserved dirty native worktree was validated without changing web source,
Supabase, Render, Vercel, or App Store Connect. Swift Core passed `12/12`, the
iOS 26.5 simulator unit suite passed `240/240`, the simulator UI suite passed
`22/22`, localization coverage passed for 844 statically discovered keys, and
the Release auth-configuration self-test passed. The current UI suite confirms
the useful More routes, guided all-notes score practice, three-octave Scale
option, cleaned score actions, and Drone -> Progress -> Practice reset behavior.

A fresh XcodeBuildMCP current-source build/run visually exposed Practice,
More, Settings, Apple/Google sign-in controls, and the three-octave Scale UI.
Computer Use opened Drone, switched to Progress, and returned to the Practice
home cards. The dedicated `BrassTuneAppUISmoke` replacement for the previously
interrupted broad journey then passed `5/5` in 156.8 seconds: More routing,
score viewer/actions, guided all-notes score practice, playable Tuba D-major
three-octave visual timing, and the same tab-reset regression. Evidence is
preserved at `NativeIOSContinuation-2026-08-05/MacIPhoneWorkarounds/SimulatorCurrentSourceFocusedNativeJourneys.xcresult`
with five current-source screenshots. This is simulator UI evidence and does
not close real Files/Photos providers, Share Sheet delivery, or physical route
and accessibility gates.

On the wired iPhone 15 Pro Max (`iPhone16,2`) running iOS 26.4.1 (`23E254`),
the Debug `.dev` target passed `6/6` focused physical UI tests in 648.783
seconds. The audio scenario repeated 20 Tuner, 20 microphone Scale, 20
Metronome, 20 reference-tone, and 20 Drone start/stop cycles, background and
foreground recovery, and 50 cross-feature switches with no app crash or test
failure. Strict Light/Dark accessibility structure, More navigation, guided
score practice, three-octave Scales, and Practice-tab reset also passed. The
result is preserved at
`<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/NativeIOSContinuation-2026-08-05/PhysicalTargetedUI-r3.xcresult`.
Two preceding attempts timed out before any test ran while enabling XCTest
automation mode. Both infrastructure results are preserved; after the stale
device-side test services were restarted and the automation prompt accepted,
the complete third attempt ran and passed.

The successful suite was followed by one iOS SpringBoard `EXC_BAD_ACCESS`
inside `XCTAutomationSession` while the test service detached. The report has
no BrassTune frame and none of the three original audio-family signatures. It
is preserved under `postflight-system-reports/`; after SpringBoard restarted,
the focused navigation recovery test passed `1/1` and the final device crash
inventory contained no additional report. The production TestFlight bundle
remained installed as `com.aryasalem.BrassTune` `1.0.0 (2)`; only the separate
`.dev` bundle and XCTest runner were exercised.

The same physical Debug `.dev` build then passed a host-coordinated real
media-services reset. While Tuner capture was active, the host sent `SIGKILL`
to the physical device's `audiomxd` process. iOS restarted it under a new PID;
BrassTune discarded the stale live graph, showed no Ready state or fabricated
measurements, restarted Tuner on retry, and then started/stopped both a
reference tone and the Metronome on fresh output graphs. The focused test
passed `1/1` in 53.665 seconds. Evidence: `audiomxd-sigkill.json`,
`device-processes-pre-media-reset.json`,
`device-processes-post-media-reset.json`, and
`PhysicalMediaServicesReset.xcresult` under the same evidence root.

Control Center exposed a real private HomePod mini destination. A
`PHYSICAL_RELEASE`-only route picker and inactive-scene exception kept the
instrumented Debug `.dev` graph alive while the system route sheet was
presented. Reference-tone and Metronome controls remained responsive through
Speaker -> HomePod -> Speaker selection, ownership released, and no matching
crash report appeared (`1/1` each, 81.858 and 92.685 seconds). These assertions
do not independently prove audible HomePod output, app-observed
`AVAudioSession.currentRoute`, or normal Release scene handling; production
AirPlay acceptance remains open. Earlier isolation attempts are retained as
harness diagnostics because the system sheet intercepted subsequent taps. The
installed-app inventory still records both bundles as `1.0.0 (2)`. Evidence:
`PhysicalAirPlayAvailabilityProbe.xcresult`,
`PhysicalActiveReferenceToneAirPlayRoundTrip-r10.xcresult`,
`PhysicalActiveMetronomeAirPlayRoundTrip-r11.xcresult`,
`PhysicalActiveReferenceToneAirPlayIsolation-r9-exported-attachments/`,
`device-crash-files-final-after-validated-active-airplay.json`, and
`installed-apps-final-after-validated-active-airplay.json`.

A fresh no-install production Release build for `com.aryasalem.BrassTune`
`1.0.0 (2)` also passed. Its strict code-signature verification and designated
requirement passed, app and dSYM UUIDs match
`995EADA0-96A2-3B5A-AA42-6507F87E3559`, and both the app and the explicit
`BrassTune App Store Distribution 2026` profile contain Sign in with Apple
`Default`, `get-task-allow=false`, and `beta-reports-active=true`. No archive,
export, install of the production bundle, upload, build 3, or release folder
was created.

After the compile-gated physical route-picker seam was added, a final generic
iOS-device Release compile with code signing disabled also passed with zero
errors or warnings. It retained `com.aryasalem.BrassTune` `1.0.0 (2)`, produced
matching app/dSYM UUID `8CAEB943-3226-3E02-9239-DDA32287F2E8`, and contained no
`PhysicalReleaseAudioRoutePicker` or `physicalRoutePickerTestMode` symbol. A
distribution re-sign attempt reached a macOS Keychain prompt attached to Codex;
Computer Use cannot control that host app, so the blocked re-sign was
interrupted and replaced with the complete unsigned compile. The earlier
distribution-signed Release result remains the signing evidence. Evidence:
`ProductionReleaseCompile-final-source.xcresult` and
`DerivedData-Production-Unsigned-FinalSource/`.

Build 3 is **Waiting for Review** after the owner directed App Review
submission with the documented residual risks still open; the app is not yet
released. The three trace-backed build-2 crash families are
closed on the exact installed build-3 payload for the tested built-in route and
all three screen-recording conditions; the real media-services reset also
passes against the repaired `.dev` source. AirPlay evidence remains
instrumented route-selection survival only. Bluetooth HFP/A2DP, wired
accessories, production audible AirPlay, calibrated real-brass tuner
accuracy/latency, accepted audible Metronome jitter, remaining exact-candidate
accessibility/import/provider acceptance, and the dirty source binding remain
open. The SpringBoard/XCTest service crash is an environment-level P2
observation, not a BrassTune source failure.
