# Release Test Matrix

> Local-only evidence registry. It contains no credentials or raw identity data,
> but user-home paths must be normalized before external publication.

Updated: 2026-08-06. The hosted backend is verified at merge revision `1cd195ed75dee61420d497438f4a4b54c07be25f` and the hosted frontend at `288d83091616acc0d906869bb6389721ac3a6017`. Native build `1.0.0 (3)` was archived from the preserved uncommitted worktree based on `0c03bb54595567166a145a9a2640f0f946432e11`, exported, uploaded, processed, assigned to internal QA, selected for version 1.0, and submitted to App Review at the owner's direction. App Store Connect reports **Waiting for Review**; manual release remains selected and the app is not public. The exact archive executable was also exported with a release-testing signature, installed on the wired iPhone, launched 20 times, and exercised through three complete physical audio matrices after XCTest authorization recovered. All three original crash families are closed for build 3 on the tested built-in route and screen-recording conditions. The owner-confirmed DSA non-trader declaration is Active for all 27 EU countries/regions. Documented P1 validation gaps remain open and were not relabeled as passes.

| Gate | Evidence | Status | Release meaning |
|---|---|---|---|
| Backend hardening | Exact candidate `305 passed, 11 skipped`; focused proxy `4/4`, resource-close `1/1`, independent proxy/header/delete `11/11`, audio hardening `35/35` | Passed and deployed | PR 26 deployed the same-origin private-audio proxy at `1cd195ed75dee61420d497438f4a4b54c07be25f`; authenticated lifecycle acceptance remains separate. |
| Frontend units | `275` passed | Passed locally | Current preserved-worktree result includes executable signed-in/guest audio-deletion behavior coverage; hosted exact-SHA evidence remains separate. |
| Frontend focused regressions | `30` passed | Passed locally | Focused result is separate from the full `275/275` frontend run. |
| Frontend production build | Passed | Passed locally | Corroborated by Vercel's exact-SHA production identity below. |
| Local Playwright | Fresh isolated full matrix: 443 passed, 7 skipped, 0 failed, total 450, 7.3 minutes | Passed locally | Evidence: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/ReleaseCandidateAutonomous-2026-08-03/final-verification/current-2026-08-04/playwright-local-fresh-isolated.log`, on isolated ports 5183/8010. The warm-up harness now uses deterministic focus plus Enter because headless WebKit intermittently lost pointer activation. The consent scenario uses an exact fallback because the stale reused backend exhausted its 10-class ownership quota; fresh unique database/ports passed. |
| Device simulation | 13 configured viewports passed | Passed locally | Not physical-device evidence. The successful rerun isolated a stale local server and reused the repository Python environment from the clean worktree. |
| Swift Core | `12/12` passed | Passed locally | Current shared-domain pass; not physical playability/editorial evidence. |
| Native unit tests | `240/240` passed, zero failures/skips | Passed in simulator | Current post-review suite includes the deterministic listen-back wait repair and a production-bundle isolation regression for auth-probe launch overrides. Evidence: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/ReleaseCandidateAutonomous-2026-08-04/simulator/auth-probe-regression/BrassTuneApp-Unit-Hardened-Final-R3.xcresult`; this remains simulator-only evidence. |
| Native full UI smoke | `22/22` passed, zero failures/skips | Passed in simulator | Final post-stable-and-estimating-gates result on iPhone 17 Pro / iOS 26.5 (start `1785824150.84`, finish `1785824757.484`): `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/ReleaseCandidateAutonomous-2026-08-03/simulator/current-ui-continuation-2026-08-04/BrassTuneAppUISmoke-post-stable-and-estimating-gates.xcresult`; includes estimating-hidden and stable-visible tests. A current XcodeBuildMCP wrapper call exceeded its 600-second wait, but retained log `build_sim_2026-08-04T21-16-19-982Z_pid61223_b8b92954.log` passed the build and `test_sim_2026-08-04T21-16-48-645Z_pid61223_6562f5ee.log` later recorded `22/22` passed, zero failures, 598.950 test seconds / 609.114 elapsed. Log SHA-256 `0bc18f5fc7823c2d7e3fd2bf9f187aa0f2e4cf278631cff09399c1b9f6cfb7c2`, Info.plist SHA-256 `855af0b7d44d5dd0c2bc238cbc26390b7c7d80c500d21487114d1ba8a30b4074`. |
| Native Thread Sanitizer | `239/239` passed, zero failures/skips; no TSan report | Passed in simulator | Full unit suite on iPhone 17 Pro / iOS 26.5 with `-enableThreadSanitizer YES`; stable evidence at `simulator/current-tsan-239-2026-08-03/BrassTuneApp-Unit-TSan-239.xcresult`. This closes the pending sanitizer rerun for the two added deterministic audio diagnostics tests, but does not replace physical route/reset stress. |
| Native plugin build/run | Debug `.dev` build installed and launched on iPhone 17 Pro / iOS 26.5 | Passed in simulator | XcodeBuildMCP rebuilt and launched `com.aryasalem.BrassTune.dev` in 14.0 seconds; the captured Practice home exposed Warm-up, Guided Practice, Metronome, Drone & intervals, and the five-tab shell. A fresh visible-card journey reached Drone, switched to Progress, then returned through Practice to `screen.practice`. Build log: `<user-home>/Library/Developer/XcodeBuildMCP/workspaces/BrassTune-e53945a1457a/logs/build_run_sim_2026-08-04T22-16-16-710Z_pid61223_f0e4ea35.log`. |
| Native shared-button icon inset | Shared primary/secondary styles use the standard `BTSpacing.lg` inset; the intentionally dense three-control metronome row uses `BTSpacing.xs` | Passed in simulator | Warm-up and Guided Practice were visually verified on iPhone 17 Pro and a newly created iPhone SE (3rd generation) / iOS 26.5 simulator. Compact QA caught and repaired a real Down/Tap tempo/Up wrapping regression. Focused iPhone 17 Pro UI was `4/4`; on iPhone SE, maximum-Dynamic-Type/RTL primary-target and metronome quick-tool tests passed `2/2`, two older tests exposed below-fold harness assumptions, and the exact failed pair passed `2/2` after the narrow `bringIntoView` harness repair. Evidence: `test_sim_2026-08-04T23-10-47-276Z_pid61223_7662ecd3.xcresult`, `test_sim_2026-08-04T23-21-43-768Z_pid61223_c567c829.xcresult`, and `test_sim_2026-08-04T23-24-40-426Z_pid61223_e8ba828c.xcresult`. This is simulator layout evidence, not a physical-device accessibility pass. |
| Native focused regressions | Audio pipeline, route/reset, auth/persistence, Scores, scale ordering, and UI fixes passed | Passed locally | Results `/tmp/BrassTune-manual-targeted-20260728-p1-source.xcresult`, `/tmp/BrassTune-manual-route-state-20260728.xcresult`, and `/tmp/BrassTune-manual-ui-targeted-20260728-current-r3.xcresult`. |
| Persistence migration write-back | Legacy metronome migration, failed-write rearm/account second relaunch, and shipping fixture quarantine second relaunch | `3/3` passed; NATIVE-024 fixed in current worktree | The restored-snapshot token defers the repaired snapshot until namespace identity permits writes, re-arms after a failed async write, and persists before a second relaunch. Evidence: `simulator/BrassTune-NATIVE024-20260803.xcresult`; also included in native `240/240`. The fix remains part of the dirty uncommitted native source, so reproducibility is still open. |
| Native targeted UI tests | `7/7` passed, zero failures/skips | Passed in simulator | Merged suite at `/tmp/BrassTuneFinalMergedUI-20260802.xcresult`, covering the prior six plus guided background regression; not a replacement for the full physical/device matrix. |
| Guided background UI regression | Prior `1/1` pass | Historical precursor | `/tmp/BrassTune-GuidedBackgroundUI-20260802-run5.xcresult`; now incorporated in the current merged `7/7` suite. |
| Physical VoiceOver | User-reported pass | Reported; not independently observed | This pass is not agent-collected evidence and does not close the broader accessibility or release gate. |
| Simulator Release builds | Current iPhone 17 Pro and iPad Pro 13-inch (M5) builds passed | Passed in simulator | Does not prove archive signing or hardware behavior. XcodeBuildMCP logs `build_sim_2026-07-29T01-52-40-519Z_pid19674_684542e1.log` and `build_sim_2026-07-29T01-53-19-747Z_pid19674_86249043.log`. |
| Generic-device Release compile | `iphoneos` arm64 build passed with signing disabled | Passed locally | Compile-only evidence; log `/tmp/BrassTune-manual-generic-device-20260728.log`. Not an install, signature, archive, or hardware claim. |
| Signed generic-device physical runner compile | Exact signed generic-device `PHYSICAL_RELEASE` runner compile passed | Passed locally | Compile evidence only; it does not establish provider completion, installation, archive, upload, or App Store readiness. |
| Localization | `844` static keys / `980` catalog entries across 12 locales; all automated checks clean | Passed locally | Automated coverage only; no human linguistic review is recorded. |
| Dependency audit | `npm audit --omit=dev` retains two high-severity React Router entries after exact pin to `react-router-dom@7.18.2` / `react-router@7.18.2` | Residual maintenance gate | The published advisory concerns unstable React Server Components APIs; this Vite client SPA does not use those APIs. `react-router-dom@8.3.0` is unavailable and React Router core v8 requires React 19.2.7+/Node 22.22+, so migration is not a narrow release-pass repair. The audit remains visible rather than suppressed. |
| GitHub Actions | Deploy workflow briefly enabled; exact-SHA dispatch remained queued, was canceled, and workflow was disabled again | Not used for deployment | Direct Vercel production redeploy was used after the GitHub-hosted runner did not start. |
| Supabase live configuration | Active project checked by CLI and Safari; migrations match through `20260724072904`; private `session-audio` bucket and RLS/grant boundaries verified | Redirect policy, native providers, and Apple web lifecycle passed; private-audio/account acceptance remains open | The live eight-entry redirect policy remains narrow. Apple Developer has enabled web Sign in with Apple for `com.aryasalem.BrassTune.web`, the production Supabase domain/callback, and a dedicated key. The Supabase Apple provider persists the web Services ID first, the production and `.dev` native App IDs, and a masked client secret. A fresh Safari Apple sign-in completed provider authorization and callback, created an Apple social-auth identity, restored after reload, signed out, and remained signed out after another reload. No database schema, Storage, redirect-policy, or RLS/grant mutation occurred in this configuration pass; the ordinary Auth identity/session creation is recorded separately as production test data. Evidence: `live-auth-redirect-reconciliation-2026-08-03/` and `LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`. |
| Render deployment | `dep-d9p5j97avr4c73amdkjg` live at `1cd195ed75dee61420d497438f4a4b54c07be25f` | Passed exact deployment identity | Hosted `45/5` and signed-out playback/export `401` checks pass. Authenticated private-audio lifecycle remains pending. |
| Same-origin private-audio proxy/delete | Exact PR 26 candidate; backend focused `4/4`, resource-close `1/1`, proxy/header/delete `11/11`; frontend deletion `30/30`, audit `34/34`, guest-audio Playwright `20/20`, TypeScript/build passed | Deployed; authenticated acceptance pending | Signed-out playback/export return `401`. Do not close NATIVE-023 or NATIVE-039 until owner playback/export/delete, cross-user denial, Storage absence, and account deletion pass. |
| Vercel deployment | `Do4tBvgN2svvYtQACwgzrxXZE5PK` Ready; [canonical production URL](https://brasstune.vercel.app); exact frontend SHA `288d83091616acc0d906869bb6389721ac3a6017` | Passed | Production `VITE_AUTH_APPLE_ENABLED=true` was verified and a full no-cache rebuild performed. |
| Hosted infrastructure smoke | `45 passed, 5 skipped` across five projects after exact deployed-SHA binding | Passed | The deployed frontend SHA was `288d83091616acc0d906869bb6389721ac3a6017`. Fresh Chromium, Firefox, WebKit, mobile Chromium, and mobile WebKit all verified enabled Google, Apple, and email entry points. This is hosted browser/infrastructure evidence, not a completed OAuth callback or signed-in private-audio lifecycle check. |
| Strict hosted browser content | `5/5` passed with strict hosted-content mode | Passed | Confirms the strict hosted-content subset at the same deployed SHA; it is not physical-device audio evidence. |
| Post-deploy error scan | No matching Render `error`, traceback, HTTP 500, or `apikey` log entries in the checked window; final Vercel build Ready | Passed | Point-in-time provider evidence only. |
| Physical Apple/Google provider initiation | Prior hardened Debug `.dev` physical XCTest `3/3`, plus current provider discovery `1/1`, Apple consent `2/2`, and Google consent `1/1` on iPhone 15 Pro Max / iOS 26.4.1 | Passed initiation surface only | Current source rendered both production-backed provider controls. Apple launched the native AuthKit authorization action twice consecutively after a narrow iOS 26 stable-identifier harness repair; Google presented the BrassTune-specific system web-auth consent alert. All probes targeted `com.aryasalem.BrassTune.dev` and stopped before accepting consent, account selection, token exchange, or callback. Current result bundles were retained in the existing private, non-repository ReleaseEvidence area; use the sanitized `LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md` record for publication. Raw identity-bearing diagnostics remain private. |
| Native Apple/Google lifecycle | Real physical `.dev` Google R7 and Apple R8, plus current attended Apple repeat `1/1` | Passed for both native providers; Apple web lifecycle also passed | Each native provider completed its real callback, generic signed-in state, cold restore, sign-out, and signed-out relaunch on the wired iPhone 15 Pro Max. The current Apple repeat passed the same sequence in 203.336 seconds. The separate Safari Apple authorization/callback/session-restore/sign-out/signed-out-reload sequence also passed and the production dashboard classified the new identity as Apple-backed. Raw identity-bearing results remain private; use only `LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`. |
| Release configuration preflight | Self-tests, configured archive-mode public-value check, and read-only identity comparison passed | Passed locally/read-only | Release is `production`, API origin is the approved Render URL, bundle ID is `com.aryasalem.BrassTune`, and the client key has public publishable shape. Current Release settings, the repository CLI link, checked-in project config, the existing signed build, and the HTTP-200 public auth endpoint resolve to the same production project without changing it. |
| Supabase project identity | Current Release settings, repository link/config, signed app, Render backend, and public auth endpoint agree | Identity, exposed-secret rotation, redirect policy, native-provider lifecycle, and Apple web lifecycle passed; storage/account lifecycle remains open | Public provider state reports Google and Apple enabled. Direct read-only Admin and Storage requests returned HTTP 200. The exact eight-entry production/development redirect allowlist remains in place. Real physical `.dev` Google R7, Apple R8, and the current attended Apple repeat callback/session/restore/sign-out journeys passed. Apple web Services ID/domain/callback/key/client-secret configuration persists, and a fresh Safari Apple authorization completed callback, restore, sign-out, and signed-out reload. Signed-in Storage/account deletion and live cross-user/RLS acceptance remain open. |
| Preserved build-2 archive | Signed `1.0.0 (2)` archive, bundle `com.aryasalem.BrassTune`, dSYM/app/crash UUID match, embedded provisioning, privacy manifest | Diagnostic evidence only | Build 2 has five trace-backed audio crashes and is not a release candidate. Archive: `<user-home>/Library/Developer/Xcode/Archives/2026-07-28/BrassTuneApp 7-28-26, 5.05 PM.xcarchive`. |
| Build-3 App Store candidate | Exact r2 archive/export/upload for `com.aryasalem.BrassTune` `1.0.0 (3)`; strict signing, profile, entitlements, privacy-manifest, archive/dSYM UUID, and upload checks passed | Uploaded and processed; internal candidate ready | Archive `BrassTune-1.0.0-3-r2.xcarchive`; IPA SHA-256 `995eac30b0be666e1768c4de0fee081e33f35208ff005beb8b3991d02ce19076`; app/dSYM UUID `A2BFB5CF-B1A2-36DB-A1FE-2A891C4133A1`. App Store Connect shows build 3 Ready to Submit, assigned to BrassTune Internal QA and selected for version 1.0. Evidence: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/AppStoreCandidate-2026-08-06-Build3`. |
| Build-3 release-testing physical install | The r2 archive was re-exported with the team Ad Hoc profile for the USB phone; stripping signatures produces a byte-identical Mach-O to the App Store archive | Installed and launch-smoke passed; not processed-TestFlight package proof | Production bundle `com.aryasalem.BrassTune` now reports `1.0.0 (3)`. Twenty terminate-and-relaunch cycles succeeded and the postflight systemCrashLogs inventory contains no new BrassTune report. This proves the exact Release executable payload under a different install signature, not TestFlight thinning, receipt, or candidate-bound feature interaction. Evidence: `PhysicalLaunchStress/` under the build-3 root. |
| Modern Supabase secret-key headers | Identity deletion and audio Storage send the server secret only as `apikey`; user-token flows preserve the user bearer token | Exact repair and proxy deployed; signed-in lifecycle pending | The header repair from PR 25 is included in backend SHA `1cd195ed75dee61420d497438f4a4b54c07be25f`; PR 26 added the same-origin proxy/delete path and Render `dep-d9p5j97avr4c73amdkjg` serves that revision. Hosted `45/5` and signed-out playback/export `401` checks pass. Signed-in audio/account-lifecycle checks remain open. |
| Live native provider discovery | Fresh `.dev` simulator launch without `UITEST_AUTH_PROVIDERS`; Sign in and Create free account inspected through accessibility | Passed provider-surface gate; token/session completion pending | Live Supabase settings report Apple and Google enabled. Both native entry paths rendered `gateway.authAppleSignIn` and `gateway.authGoogleSignIn`; production and `.dev` Google authorization-start probes returned provider redirects. No account, consent, token exchange, user, or session was created. Evidence: `post-deploy-eb4dac6/`. |
| Current native/UI continuation | iOS 26.5 unit `240/240` and final UI `22/22`; iOS 26.2 live-provider surface; manual navigation and scale checks | Passed for simulator scope | Current `.dev` source built/ran through XcodeBuildMCP. Apple and Google rendered without fixture flags; More exposed Settings, Privacy, and Support; Three octaves selected; Drone -> Progress -> Practice returned to Practice home. The two Scale discard-diagnostics tests also passed with `PHYSICAL_INSTRUMENTATION`; the final UI suite includes estimating-hidden and stable-visible tests. Evidence: `live-dashboard-audit-2026-08-03/`, `simulator/post-scale-diagnostics-2026-08-03-r2/`, and `simulator/current-ui-continuation-2026-08-04/`. No provider button, account, production bundle, archive, or upload was touched. |
| Simulator memgraph | Guest Scales-flow memgraph; `leaks --list` reported 0 leaks / 0 bytes | Passed for one bounded simulator snapshot | Evidence: `memgraph-2026-08-04/`. This is not physical microphone/audio-route/authenticated-lifecycle leak evidence. |
| Exact primary-navigation regression | iPad Pro 13-inch (M5) / iOS 26.2 `1/1`; iPhone 17 Pro / iOS 26.2 `1/1` | Passed in simulator | `simulator/ipad-navigation-2026-08-03/`. The iPad test seam verifies the five labeled floating-strip controls in order; the iPhone path retains the native tab-bar assertion. Not physical-iPad usability evidence. |
| Physical maximum Dynamic Type + RTL | iPhone 15 Pro Max `.dev` `1/1`, 68.661 s | Passed for this automated physical scenario | `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/NativeContinuation-2026-08-03.wbWCWD/physical-max-dynamic-type-rtl.xcresult`; production bundle remained untouched. This does not replace Voice Control, system-setting, physical-iPad, or uncoached-usability checks. |
| Current-source physical performance | Bound Release-optimized `.dev` run: 20/20/20/20/20 starts + 50 switches; 30/30 Scale completions, 60 frames, zero drop events/failures; Time Profiler zero >=250 ms stalls | Passed for one bound physical observation | Evidence `physical/current-source-performance-2026-08-03/README.md` and `metrics.json`: app-input manifest `f0021dd7c4184d089a323051c1a7373e28a78bfc72e30307bdb10db99950c4f1`, binary SHA `4376fe518274061360be8bd7ffa0459b1c7d43b3a12d17d561ffd3bd9679ea5c`, UUID `237789E0-032B-3781-AF8B-68003568A0F8`; CPU mean 14.744%, p95 33.073%, peak 40.163%; real memory 60.875-67.953 MiB; post-install/warm launch 643.064/507.547 ms. This manifest predates the later display-only AppRootView/UI-test patch, so it does not bind the entire final dirty worktree. It remains one device/run, not a battery, reboot-cold-launch, or statistical-population claim. |
| Metronome analyzer | `11/11` deterministic tests, independent science review approved | Passed for analyzer contract; physical jitter fail-closed | The analyzer now uses detector-window timestamps/SNR windows and hop-scale thresholds. It rejects missing/mismatched calibration, crosstalk, tampering, and alternating jitter; a valid calibrated fixture remains `passed: false` until valid physical multichannel calibrated evidence exists. Do not report an audible jitter measurement. |
| Expanded synthetic tuner sweep | 13,920 steady synthetic cases + 132 onsets; aggregate gates passed; focused native contract `1/1` | Passed aggregate synthetic contract; per-profile risk open | `synthetic-tuner-expanded-2026-08-03/README.md` and `summary.json`. Aggregate lock 100%, nearest-note 99.9928%, p95 2.2093 cents; onset p95 278.639 ms. Tuba p95 8.9947 cents exceeds the shared 8-cent gate and one low-register synthetic case flips a nearest note identically in backend/frontend/native while remaining below recording confidence. This is a deterministic robustness finding, not physical accuracy/latency evidence. |
| Native tuner display gating | Focused repair plus final full native UI `22/22` | Passed in simulator; physical provisional-display validation open | The final suite verifies both estimating-hidden and stable-visible simulator states. It does not replace a physical display-gating journey or calibrated tuner validation. |
| Internal candidate and App Store submission | Build 3 is uploaded, processed, assigned to internal QA, selected for version 1.0, added to a review submission, and submitted | **Waiting for Review; not released** | Product metadata, screenshots, review information, category, 4+ age rating, content rights, free worldwide pricing, build selection, published privacy disclosure, and the owner-confirmed DSA non-trader declaration are complete. The owner directed submission with the documented P1 gaps still open; those gaps remain residual risks rather than passed evidence. Manual release remains selected. |
| Production Sign in with Apple provisioning | Explicit App Store profile and Apple Distribution identity produced the uploaded build-3 archive | Passed through upload | The r2 archive contains Sign in with Apple `Default`, `get-task-allow=false`, `beta-reports-active=true`, the production bundle ID, strict-valid signature, and matching app/dSYM UUID. App Store Connect accepted and processed the exact upload. Evidence: build-3 root `ArchiveBuild3-r2.xcresult`, archive, export, and upload metadata. |
| Physical device preflight | iPhone 15 Pro Max (`iPhone16,2`), iOS 26.4.1 (`23E254`), wired, connected/trusted/unlocked, Developer Mode; Xcode 26.6 (`17F113`) | Passed | Confirmed physical hardware, not a simulator. Stable device identifiers remain only in local evidence. |
| Physical isolated `.dev` installs | Artifact-bound August 2 Release and Release-instrumented `com.aryasalem.BrassTune.dev` `1.0.0 (2)` builds | Passed for those artifacts | Only the isolated `.dev` bundle was replaced for testing. Production TestFlight `com.aryasalem.BrassTune` `1.0.0 (2)` remained separately installed and untouched. These artifacts predate later UI-test/route-seam source and are not exact-current-candidate evidence; no archive/upload occurred. |
| Final Release-equivalent device build | Final Release-optimized app preserved under the August 2 new-phone audit root, correctly isolated as `com.aryasalem.BrassTune.dev` | Build/install/Runner passed | The signed app installed beside the untouched production bundle and passed the exact-source physical crash-family Runner. Preserved root: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/ReleaseCandidateAudit-2026-08-02/NewPhone-i15PM`. This is not provider-flow, archive, upload, or full App Store readiness proof. |
| Current full physical UI plan | Rebuilt current-source Release `.dev` runner passed `20/20`, zero failures/skips | Passed for automated fixture scope | Covers onboarding, navigation, guided all-notes practice, score viewer/actions, background recovery, legal/settings/export scope, account/provider presentation, destructive confirmations, maximum-Dynamic-Type fixture, and RTL fixture. Evidence: `physical/ui/release-dev-standard-20-after-accessibility-repair.xcresult`. Not complete assistive-technology, real-provider auth, or real Files/Photos proof. |
| Physical Control Center screen-recorder variants | Debug `.dev` stress with narration microphone off: `1/1` passed in 501.462 seconds; narration microphone on: `1/1` passed in 501.617 seconds | Passed for both original recording conditions | Each run completed 20 Tuner, 20 microphone Scale, 20 Metronome, 20 reference-tone starts, Tuner/Metronome background recovery, and 50 rapid primary-tab navigations. The tab loop did not start another 50 audio features. Control Center option/active/stop screenshots, structured results, postflight inventories, and the two full physical recordings are preserved in `Continuation-DgxbMK`. The tested Debug binary UUID is `ED78D9ED-88EC-34DC-A9A8-75AF664CE9F9`, SHA-256 `bebfa28d585b8663cc35ed2dfc68cdc5589042782c66e675e80487a3e812d26f`; Debug emitted embedded DWARF and no separate dSYM. |
| Physical metronome repeated cycle | Artifact-bound August 2 Release Runner completed 20 cycles plus Metronome use inside 50 cross-feature switches; screen-recorder variants and sustained runs also passed | Build-2 crash family closed for the built-in route; timing gate open | No `ScheduleBuffer`/`NativeMetronomeOutput` failure or crash occurred, and the post-run scan contains no matching signature. A new 2026-08-05 Debug `.dev` built-in-speaker run lasted 180 seconds and also passed without a crash. These artifacts are not exact-current-candidate evidence. |
| Physical real tuner repeated cycle | Artifact-bound August 2 Release Runner completed 20 cycles plus Tuner use inside 50 switches; screen-recorder, permission recovery, media reset, and sustained A440 runs also passed | Build-2 input-tap trap family closed for the built-in route; broader tuner gate open | Zero failures/drop events and no `dispatch_assert_queue` signature. The new sustained A440 run held written B4 for 40/40 half-second samples at 439.9–440.2 Hz and -1 to +2 displayed cents. Consumer Mac audio is not calibrated accuracy or source-onset latency. |
| Physical microphone Scales | Artifact-bound August 2 Release Runner completed 20 cycles plus Scale use inside 50 switches; a later Debug `.dev` rerun captured Scale drop diagnostics | Build-2 duplicate-tap family closed for the built-in route; broader Scales gate open | The diagnostic passed 20 Scale cycles plus 10 Scale transitions: 30/30 completions, 60 accepted frames, zero missing/zero-frame completion, zero input-drop events, and zero crash/assertion. Wrong-octave rejection is deterministic-test evidence; physical count-in cancel/exit/interruption breadth remains incomplete. |
| Physical reference tone and Drone | Initial run reproduced 60 `kAUStartIO` failures; repaired reruns passed 20 reference tones, 20 Drone starts, and 20 additional output starts in stress, per configuration | Repaired; lifecycle pass | A fresh output graph after capture teardown fixed the deterministic failure. Audibility, level, haptic policy, route behavior, and feedback were not measured. |
| Physical cross-feature stress | Artifact-bound August 2 Release Runner completed 50 audio-feature switches across Tuner, microphone Scale, Metronome, reference tone, and Drone after all 20-cycle groups; a later Debug `.dev` rerun repeated the counter-bearing sequence | Passed for the built-in route and original crash families | Both runs report zero failures; the later run recorded zero Tuner/Scale input-drop events. Post-run scans contain none of the three original crash signatures. The August 2 artifact is not exact-current-candidate evidence. Media reset passed later; AirPlay remains instrumented route-selection survival only. |
| Physical input-buffer drop-event/overflow instrumentation | Initial tone-repair run reported 11 failed Tuner input-buffer enqueues/copies; final capacity repair and post-patch Scale diagnostic report zero detected events | Passed for instrumented Tuner and Scale input on the exercised built-in route | The first hardware callback after output-to-input handoff exceeded the 4,096-frame slot. Preallocated capacity is now 8,192; repeated final Tuner runs and the new 20-cycle/10-switch Scale run report zero detected drop events. The Scale run proved real delivery with 60 accepted frames across 30/30 completions. The counter is event-based, not a PCM-frame total; unavailable routes remain untested. Evidence: `physical-scale-drop-2026-08-03-r2/`. |
| Physical routes and screen-recorder variants | Built-in speaker/microphone with recording off, narration microphone off/on; timer interruption; later media reset and instrumented AirPlay selection | Strong partial pass | Both original screen-recorder variants completed the focused repeated-cycle matrix plus 50 rapid tab navigations without a crash. `physical-system-timer-interruption-r2.xcresult` and the real media reset passed. The Debug `.dev` route-picker tests survived selection of a private HomePod mini destination and later iPhone Speaker with responsive controls, but did not prove audible HomePod output, app-observed route identity, or normal Release scene behavior. Wired and Bluetooth HFP/A2DP remain unavailable. |
| Physical music accuracy and latency | Controlled pure-sine playback observed on the phone through the built-in microphone under the B-flat trumpet profile | Strong partial functional evidence; not calibrated music validation | A440 produced written B4 at 440.0 Hz/0 cents. The amplified low/middle/high retry produced D4 at 261.6 Hz/0 cents, B4 at 440.0 Hz/0 cents, and A5 at 784.0 Hz/0 cents, all “In tune.” Host marker-to-lock for A440 was about 4.09 seconds and is explicitly uncalibrated. These are Mac-speaker sine observations, not real-brass, calibrated accuracy/latency, or other-instrument evidence. Evidence: `physical/music-validation/tuner-a440-acoustic.xcresult` and `physical/music-validation/tuner-low-mid-high-acoustic-retry2.xcresult`. |
| Physical Debug performance traces | Time Profiler, Allocations, Power Profiler, Audio System Trace, and Hangs captured while the optimized Debug harness passed | Failed performance gate | Approximate Time Profiler active samples total 1.141 CPU seconds over 21.10 seconds; thermal state stayed Nominal; process CPU-impact samples reached 13 without an idle baseline; all exported Audio HAL I/O-cycle buckets are Normal; HAL/client POI tables are empty. Allocations recorded but did not expose an exportable quantitative heap table. Hangs found 12 main-thread microhangs, 260.07–390.79 ms. |
| Historical Release-equivalent performance | Earlier current-source Release-instrumented trace | Historical context only | The older 45.791-second trace had 12 potential 251.135-335.598 ms main-thread stalls. It is superseded for this bound current-source workload by the later 50.836-second Time Profiler observation with zero >=250 ms potential hangs and zero hang-risk rows; it remains useful only as repair lineage. |
| Physical Release audio/render deadlines | Artifact-bound August 2 Release Audio System Trace completed against the 20/20/50 harness | Passed for the captured built-in route/window | The 20.000-second interval contains 200/200 `Normal` HAL load buckets, 32/32 `Normal` lifecycle events, no audio-client POI or hang-risk row, and Nominal thermal state. It is not exact-current-candidate evidence and does not cover accessory routes, interruptions, screen recording, or audible timing. |
| Metronome jitter | New physical 120-BPM built-in-speaker run passed `1/1` for 180 seconds; Mac microphone recorded raw 48 kHz/24-bit NUT audio | Functional/audible consumer observation; no jitter result | The raw stream has 190.586667 seconds of samples across a 230-second timeline, with 3,691 packet gaps totaling 39.412040 seconds. It contains audible 1,320 Hz click energy but lacks packet-complete click timing, independent pilot, calibration, and scheduler/signpost binding. No jitter statistic is accepted. Evidence: `NativeIOSContinuation-2026-08-05/MacIPhoneWorkarounds/`. |
| Tuner accuracy and latency | Prior low/middle/high pure-sine locks plus new sustained A440 physical display sampling | Strong partial functional evidence; calibrated metric open | New A440: written B4 for 40/40 half-second samples, 439.9–440.2 Hz, -1 to +2 displayed cents, and 1,143.1 ms steady-source acquisition. Because the tone predates capture and consumer Mac/iPhone audio is uncalibrated, this is not source-onset latency or calibrated absolute accuracy. Evidence: `MacIPhoneWorkarounds/PhysicalTunerSustainedA440DisplayStability.xcresult`. |
| Files/Photos and accessibility substitutes | Focused model tests `9/9`; simulator Increase Contrast + accessibility XXXL/RTL/strict audit `2/2`; system Reduce Motion flows `2/2`; iPhone orientation `1/1`; iPad navigation/orientation `2/2` | Stronger simulator/model partial; physical gates remain | Earlier real physical Photos/iCloud Files PNG journeys remain historical. Current-source substitutes cover deterministic export, rename/persistence, deletion, failed-photo cleanup, PNG/JPEG preservation, restore, target reachability, strict Light/Dark structure, orientation, and Reduce Motion flows. The separate broader UI journey was infrastructure-interrupted and is not counted. VoiceOver is owner-reported passed; real PDF/JPEG/HEIC/share, Voice Control, alert-focus restoration, physical system-setting perception, physical iPad, and uncoached usability remain open. Evidence: `NativeIOSContinuation-2026-08-05/MacIPhoneWorkarounds/`. |
| Physical exact-source crash-family Runner | First attempt timed out enabling automation before test execution; retry passed `1/1` in 492.429 seconds | Passed after infrastructure retry | Release `.dev` on iPhone 15 Pro Max completed Tuner 20, microphone Scale 20, Metronome 20, reference tone 20, Drone 20, and 50 cross-feature switches with zero failures and zero detected Tuner input-buffer drop events. Evidence: `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/ReleaseCandidateAudit-2026-08-02/NewPhone-i15PM/physical-release-audio-crash-family-runner-final-source-retry1.xcresult`; this is preserved August 2 audit evidence, not inside the August 3 report root. |
| Final physical crash-log scan | Final systemCrashLogs snapshot compared with preflight | Original families passed; runner artifacts classified | No `AVAudioPlayerNodeImpl::ScheduleBuffer`, `NativeMetronomeOutput`, `dispatch_assert_queue`, `AVAudioEngineGraph::InstallTapOnNode`, `InstallTapOnNode`, SIGABRT, or SIGTRAP match exists. Three `.dev` `EXC_GUARD`/LIBXPC lightweight user-fault corpses align with XCTest launch/termination, contain no usable app source frame, and are not equivalent to the build-2 audio families. Final listing found no new BrassTune report after 12:16 PM and none from the current 15:xx runs. |
| App Store Connect metadata | Build 3 selected; screenshots, product/review copy, categories, 4+ age rating, content rights, free worldwide pricing, availability, published privacy disclosure, and DSA declaration saved | Passed except accessibility declarations | App Store Connect enables Add for Review. Accessibility nutrition labels remain conservatively undeclared because the exact candidate did not receive the full applicable physical criteria run. The owner confirmed DSA non-trader status while retaining EU distribution; App Store Connect reports the requirement Active for all 27 EU countries/regions. |
| Manual secret/artifact audit | Current diff, untracked native/docs files, public-client tree/history, generic Release app, build-2 archive, and five crash ZIP payloads were inspected; tree/history matches were only deliberate rejection/test sentinels, with no credential-shaped secret or private key identified; nothing staged; no file over 20 MB outside excluded dependency/build caches | Passed for inspected artifacts | This does not prove provider-side rotation, RLS correctness, or third-party infrastructure state. |
| Restricted-provider boundary | Secret rotation/source deployment/redirect reconciliation, authorized native lifecycle, then Apple web provider configuration | Provider configuration passed; production test data and remaining lifecycle gaps explicitly recorded | The 2026-08-04 authorized Google and Apple native sign-ins plus the synthetic upload created normal production Auth/session/application/Storage test data; one synthetic recording remains. A later owner-authorized configuration pass changed Apple Developer web Sign in with Apple and the production Supabase Auth Apple provider only; it did not mutate database schema, tables/rows, Storage, redirect policy, or RLS/grants. App Store Connect was untouched. Both installed apps remain `1.0.0 (2)`, TestFlight was not replaced, the dirty worktree was not staged/reset/cleaned/stashed/rebased/overwritten, and `Release/BrassTune-v1.0/` remains absent. |

## Reproduction boundary

### 2026-08-04 live auth/storage addendum

The sanitized publishable evidence is
[`LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md`](LIVE_AUTH_STORAGE_EVIDENCE_2026-08-04.md);
raw identity-bearing native diagnostics remain private/quarantined. Physical
Google R7 and Apple R8 each passed `1/1` for real provider completion,
callback, generic signed-in state, cold restore, sign-out, and signed-out
relaunch. Apple R7 completed authorization but exposed a false-negative test
assumption: the auth sheet correctly returned to its presenting Settings
screen while the probe required Practice first. The narrow Settings-first
signed-in probe repair was rebuilt and signed; authorized-session cleanup and
signed-out preflight R8 each passed `1/1`, followed by the full Apple R8 pass.
The current-source attended physical Apple repeat also passed `1/1` in 203.336
seconds for authorization, callback, signed-in state, cold restore, sign-out,
and signed-out cold relaunch.

The production TestFlight app `com.aryasalem.BrassTune` `1.0.0 (2)` remained
installed and its installation path unchanged. The restored and launched optimized Release `.dev`
binary uses production environment and `.dev` callback settings, is
development-signed with `get-task-allow=true`, and has SHA-256
`b5a3fe82dbd2fa298a339f4ea6f187cfb13d85a7734b271d7cd334408e4a7130`.

Historical reproduction at hosted SHA `eb4dac6749fb7fd954d79ef7e90a7687a8e428c7`
passed smoke 45
with 5 skipped. An authorized ordinary-student Google session restored; one
synthetic Demo WAV uploaded with persisted session metadata and an owner-visible
row. Playback and export failed. A privacy-safe in-page probe isolated a
cross-origin redirect/data-plane failure (followed fetch `TypeError`; manual
redirect status `0`, type `opaqueredirect`). The proxy repair and signed-in
delete UI were subsequently deployed at backend SHA
`1cd195ed75dee61420d497438f4a4b54c07be25f`; authenticated owner acceptance
remains open. Web sign-out and signed-out reload passed.

The later owner-authorized pass changed only Apple Developer web Sign in with
Apple and the production Supabase Auth Apple provider. A fresh Safari Apple
authorization then completed callback, produced an Apple-backed production Auth
identity, restored the BrassTune session after reload, signed out, and remained
signed out after another reload. The configuration pass did not change the
database schema, tables/rows, Storage, redirect policy, or RLS/grants. Authorized
sign-ins did create normal production Auth, session, application, and Storage
test data. One synthetic recording remains because
authenticated deletion has not yet been accepted against the deployed control.
Live cross-user/BOLA denial, live audio
deletion, account deletion, and final Storage absence remain unverified. App
Store Connect was untouched; no build 3, archive, upload, or release folder was
created. Outcome: **BLOCKED**. Frontend is `275/275` after the signed-in
deletion behavior follow-up and its production build passed; focused frontend
regressions are `30` passed; fresh local backend hardening is `198` passed /
`11` skipped. Swift Core is `12/12`; the exact signed generic-device
`PHYSICAL_RELEASE` runner compile passed. Final crash-log listing found no new
BrassTune report after 12:16 PM or from the current 15:xx runs.

Later source changes, a deployment, or an archive require a fresh corresponding test/deployment record. Hosted passes do not establish signed, TestFlight, App Store, signed-in private-audio lifecycle, or physical-device success.

## 2026-08-05 native-only continuation

| Scenario | Result | Status | Evidence / boundary |
|---|---|---|---|
| Swift domain package | `12/12` passed | Passed | `cd swift/BrassTuneCore && swift test`. |
| Native simulator units | `240/240`, zero failures/skips on iPhone 17 Pro / iOS 26.5 simulator | Passed | `<user-home>/Library/Application Support/BrassTune/ReleaseEvidence/NativeIOSContinuation-2026-08-05/SimulatorUnit.xcresult`. |
| Native simulator UI | Earlier complete smoke `22/22`; fresh selected non-physical suite `23/23`, zero failures/skips in 721.901 test seconds | Passed | Covers current More, score-practice, guided all-notes, three-octave Scale, navigation reset, auth presentation, tuner display gating, orientation, settings, and the repaired published legal-page status. Fresh evidence: `AppStorePreflight/SimulatorUI-23.xcresult`; focused legal evidence: `AppStorePreflight/SimulatorLegalUI.xcresult`. |
| Current-source focused native journeys | `5/5`, zero failures/skips in 156.8 seconds | Passed in simulator | Dedicated `BrassTuneAppUISmoke` cases replaced the previously interrupted broad journey: More routes and delete-cancel, score viewer/action cleanup, guided all-notes score practice, playable Tuba D-major three-octave visual timing, and Drone -> Progress -> Practice-home reset. Computer Use independently repeated the rendered navigation path. Evidence: `MacIPhoneWorkarounds/SimulatorCurrentSourceFocusedNativeJourneys.xcresult` and five `CurrentSource-*.jpg` screenshots under the same root. |
| Localization and Release-auth preflight | 844 static keys; zero missing/incomplete/placeholder/terminology/semantic violations; auth self-test passed | Passed | `verify_localization_coverage.py` and `test_release_auth_config.sh`. |
| Physical targeted UI | Fresh current-source `7/7`, zero failures/skips in 663.931 test seconds on wired iPhone 15 Pro Max / iOS 26.4.1 | Passed for focused Debug `.dev` scope | Includes strict Light/Dark accessibility structure, More routes, published legal/support presentation, guided score practice, three-octave Scales, Practice reset, and the full built-in-route crash-family stress. Evidence: `AppStorePreflight/PhysicalCurrentSourceTargeted-r1.xcresult`. The earlier `PhysicalTargetedUI-r3.xcresult` remains preserved as a separate `6/6` pass. |
| XCTest automation preflight | First two attempts timed out enabling automation before any test ran; stale device test services were restarted and the automation prompt accepted | Recovered infrastructure failure | `PhysicalTargetedUI.xcresult` and `PhysicalTargetedUI-r2.xcresult` preserve both non-app failures; the complete R3 suite then passed. |
| Physical audio crash-family stress | Fresh current-source run completed 20 Tuner + 20 microphone Scale + 20 Metronome + 20 reference-tone cycles, Tuner/Metronome background/foreground recovery, and 50 rapid primary-tab switches; preserved earlier exact-source runs additionally exercised 20 Drone cycles | Passed for built-in microphone/speaker route | No BrassTune crash or XCTest assertion; the postflight inventory contains no crash report from the 2026-08-05 run, and all three original build-2 families remain closed for this route. A real media-services reset and instrumented AirPlay route-selection survival were exercised separately. Bluetooth/wired routes, production audible AirPlay, calibrated latency, and accepted audible jitter remain open. Fresh evidence: `AppStorePreflight/PhysicalCurrentSourceTargeted-r1.xcresult` and `device-crash-files-after-current-source-physical.json`. |
| Physical media-services reset | `audiomxd` restarted by host while real Tuner capture was active; Tuner stopped stale publication, remained fail-closed, retried, and reference tone + Metronome restarted | Passed `1/1` in 53.665 seconds | Physical iPhone 15 Pro Max / iOS 26.4.1, Debug `.dev`. iOS restarted `audiomxd` under a new PID. Evidence: `PhysicalMediaServicesReset.xcresult`, `audiomxd-sigkill.json`, and pre/post process inventories. No new device crash report appeared. |
| Instrumented AirPlay reference-tone route selection | `PHYSICAL_RELEASE` route picker selected Speaker -> HomePod -> Speaker while reference-tone controls remained responsive; ownership released and a fresh Drone opened | Survived `1/1` in 81.858 seconds; production audible route open | The Debug `.dev` test used a compile-only route picker and inactive-scene exception. It proves app/control survival and no matching crash report, not audible HomePod output, app-observed `AVAudioSession.currentRoute`, or normal Release scene handling. Evidence: `PhysicalActiveReferenceToneAirPlayRoundTrip-r10.xcresult`. |
| Instrumented AirPlay Metronome route selection | `PHYSICAL_RELEASE` route picker selected Speaker -> HomePod -> Speaker while BPM controls remained responsive; ownership released and a fresh Metronome opened | Survived `1/1` in 92.685 seconds; production audible route open | No `ScheduleBuffer`/`NativeMetronomeOutput` report was added, but the test did not independently verify audible HomePod output or normal Release scene behavior. Evidence: `PhysicalActiveMetronomeAirPlayRoundTrip-r11.xcresult`. |
| XCTest service postflight | One SpringBoard `EXC_BAD_ACCESS` in `XCTAutomationSession` after the successful 6-test suite; a post-restart test passed `1/1`; final inventory added no report | Environment P2; app recovery passed | Report preserved in `postflight-system-reports/SpringBoard-2026-08-05-145731.ips`; no BrassTune frame or original audio-crash signature. Recovery evidence: `PhysicalPostSpringBoardRecovery.xcresult`. |
| Production Release signing | Fresh current-source no-install `1.0.0 (2)` Apple Distribution build; zero errors/warnings; strict signature valid; Apple Sign In `Default`; `get-task-allow=false`; `beta-reports-active=true`; privacy manifest valid | Passed locally; not an archive/upload | App/dSYM UUID `6F560646-9C34-38C0-B097-B05E8B70C0EC`. Evidence: `AppStorePreflight/DistributionBuild.xcresult` and `AppStorePreflight/DerivedData-DistributionBuild/Build/Products/Release-iphoneos`. Build `2` is already used and cannot be uploaded again. |
| App Store screenshots and copy | Five opaque 6.9-inch iPhone JPEGs and two opaque 13-inch iPad JPEGs uploaded; product copy, subtitle, keywords, URLs, copyright, guest-review notes, and review contact saved | Passed live | Assets remain under `<user-home>/Library/Application Support/BrassTune/AppStoreSubmissionAssets-2026-08-05`. Build 3 is selected, manual release is configured, and the version is now Waiting for Review. |
| Live App Store Connect completion | Build 3 selected; privacy disclosure published; Music/Education categories, 4+ age rating, no-third-party-content declaration, free pricing, 175-region availability, iPhone/iPad-only storefront scope, and DSA declaration saved | Submitted; Waiting for Review | App Store Connect accepted one review item for iOS version 1.0 / build `1.0.0 (3)`, reports Waiting for Review, and says review may take up to 48 hours. Accessibility nutrition labels remain conservatively undeclared. The owner-confirmed DSA non-trader declaration is Active for all 27 EU countries/regions, and App Store Connect reports all current regulatory requirements complete. |
| Post-route-seam normal Release compile | Generic-device `com.aryasalem.BrassTune` `1.0.0 (2)` with no `PHYSICAL_RELEASE` condition; zero errors/warnings; app/dSYM UUID `8CAEB943-3226-3E02-9239-DDA32287F2E8`; route-picker symbols absent | Passed locally; unsigned compile only | Evidence: `ProductionReleaseCompile-final-source.xcresult` and `DerivedData-Production-Unsigned-FinalSource/`. It proves the normal app branch compiled after the route seam; it is not a signed archive or exact committed candidate. Later changes in this workaround segment were UI-test/docs only. |
| Production bundle postflight | Production bundle is `com.aryasalem.BrassTune` `1.0.0 (3)` from the release-testing export; `.dev` `1.0.0 (2)` remains installed separately | Passed explicit replacement boundary | The prior build-2 installation was replaced only after the owner repeatedly authorized autonomous release preparation. The installed executable is byte-identical to the r2 archive after stripping signatures; the install signature/profile differs from the processed TestFlight package. |
| Build-3 XCTest authorization recovery | Secure device authorization completed; recovery canary executed `1/1` | Passed | `TestFlightInspect-r7.xcresult` proves the signed physical runner and XCTest service recovered before candidate-bound testing. Earlier timeout/passcode artifacts remain preserved as infrastructure history, not app failures. |
| Exact build-3 baseline crash-family matrix | 20 Tuner + 20 microphone Scale + 20 Metronome + 20 Drone/reference-tone cycles, Tuner/Metronome background recovery, and 50 cross-feature switches | Passed `1/1` in 454.672 seconds | Exact installed `com.aryasalem.BrassTune` `1.0.0 (3)` production bundle; `CandidatePhysicalAudioBaseline.xcresult`. |
| Exact build-3 screen recording, microphone off | Complete 20/20/20/20 plus 50-switch matrix | Passed `1/1` in 457.460 seconds | Control Center preparation asserted microphone-off state; `ScreenRecordingMicrophoneOff-Prepare.xcresult`, `CandidatePhysicalAudio-ScreenRecordingMicOff.xcresult`, and stop result. |
| Exact build-3 screen recording, narration microphone on | Complete 20/20/20/20 plus 50-switch matrix under the original build-2 discovery condition | Passed `1/1` in 459.360 seconds | Control Center preparation asserted microphone-on state; `ScreenRecordingMicrophoneOn-Prepare.xcresult`, `CandidatePhysicalAudio-ScreenRecordingMicOn.xcresult`, and stop result. |
| Exact build-3 sustained A440 display | 40/40 half-second samples held written B4 at 439.9–440.1 Hz and 0 cents; already-present-tone lock 1,133.4 ms | Passed `1/1` | Consumer Mac-speaker reference, not calibrated source-onset latency; `CandidatePhysicalTunerA440.xcresult` and exported attachment. |
| Exact build-3 sustained Metronome | Three 180-second 120-BPM runs completed without a crash; final maximum-volume run passed in 213.718 test seconds | Passed for sustained output; jitter failed closed | The final SoX/CoreAudio capture is continuous 48 kHz/24-bit PCM over 219.52 seconds, but only 210/356 expected pulse bins cleared the lowest threshold and 834 extra candidates remained. No numerical jitter claim. Evidence: `CandidatePhysicalMetronome120-Max-SoX.xcresult`, WAV, and verdict. |
| Exact build-3 original-signature and crash postflight | Zero matches for `ScheduleBuffer`, `NativeMetronomeOutput.playTick`, `dispatch_assert_queue`, `InstallTapOnNode`, `configureAndStartLiveEngine`, `SIGABRT`, or `SIGTRAP`; zero new BrassTune device reports since matrix start | Passed | `PhysicalCandidate-original-signature-scan-clean.txt` and `PhysicalCandidateCrashLogs-post-A440-summary.txt`. The one new SpringBoard report is XCTest infrastructure and contains no BrassTune report identity. |
| Post-instrumented-route device logs | No systemCrashLogs item was created after the two route-selection tests; both explicitly selected iPhone Speaker at the end | Passed for crash-log absence only | This does not prove the effective audio route or audible output. Evidence: `device-crash-files-final-after-validated-active-airplay.json` and the two focused result bundles. |
| App Store release decision | Build 3 archive/upload/processing, metadata, DSA declaration, exact-candidate built-in-route audio P0, and App Review submission are complete; documented P1 validation gaps remain | **Waiting for Review; manual release pending** | Twenty physical launch cycles, three complete candidate audio matrices, sustained A440, exact-signature scan, and final device crash inventory passed. Original crash families A/B/C are closed for the built-in route and original screen-recording conditions. Bluetooth/wired/production-audible-AirPlay, calibrated latency/jitter, exact-candidate Instruments, Files/Photos, full accessibility/provider lifecycle, and dirty source binding remain open. The owner directed submission with those risks documented; the app is not released and no release folder was created. |
