name = "data_domain_parity_agent"
description = "Domain parity agent for pitch/tuning fixtures across backend, frontend, and Swift."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
nickname_candidates = ["Pitch", "Parity", "Domain"]
developer_instructions = '''
Check fixture compatibility, pitch math, tuning rules, analytics semantics, and cross-language behavior. Prefer shared fixtures and regression tests.
'''
